# Phase 03: AI IOC Extraction — Handoff

**Paused:** 2026-06-25T14:50:00Z
**Status:** Planning not started — directory created, researcher interrupted before running

---

## This Session

- **Phase 2 UAT: COMPLETE** — 14/14 tests passed (commit 5308547)
  - Cold start smoke test ✓
  - URLhaus IOCs visible in OpenCTI ✓
  - Feodo IOCs (ipv4-addr:value) visible ✓
  - Confidence Score=45 on URLhaus IOC (D-09 formula verified) ✓
  - MalwareBazaar/ThreatFox gracefully disabled ✓
  - OTX active (status=ok) ✓
  - pytest 25/25 ✓
  - Redis dedup keys: tim:ioc_seen:<sha256-hex> ✓
  - BaseFeed importable inside container ✓
- Phase 3 directory created: `.planning/phases/03-ai-ioc-extraction/`
- `/gsd-plan-phase 3` interrupted before researcher ran — nothing to resume

---

## Resume With

```
/gsd-plan-phase 3
```

This will:
1. Spawn `gsd-phase-researcher` to research intel-extractor implementation (Ollama/llama3.2:3b, PDF/URL ingestion, STIX output, ATT&CK mapping, chunking)
2. Spawn `gsd-pattern-mapper` to map Phase 2 patterns to Phase 3 analogs
3. Spawn `gsd-planner` to create PLAN.md files
4. Spawn `gsd-plan-checker` to verify plan quality

---

## Phase 3 Goal

> An analyst can submit a PDF file or a URL to the intel-extractor service and receive extracted IOCs — mapped to MITRE ATT&CK techniques where mentioned — inserted into OpenCTI as STIX objects, processed entirely by the local LLM.

**Requirements:** AIEX-01 (PDF), AIEX-02 (URL), AIEX-03 (ATT&CK mapping), AIEX-04 (STIX into OpenCTI), AIEX-05 (chunking for long docs)

**Depends on:** Phase 1 only (not Phase 2)

**Key constraints:**
- Local LLM only — Ollama with llama3.2:3b (already pulled)
- 4GB VRAM — one model active at a time
- No external API calls

---

## Running Background Processes

- `feed-orchestrator` healthy — URLhaus(1h), Feodo(4h), OTX(6h) schedules active
- OpenCTI at localhost:8080 with 22,656+ IOCs indexed

---

## Advisory Constraints (carry forward from Phase 2)

- **BC-01**: feed-orchestrator docker compose always needs `--profile platform --profile feeds`
- **BC-02**: pycti 6.4.x rejects externalReferences as dicts — use ID strings
- **BC-03**: URLhaus CSV needs explicit DictReader fieldnames (no non-comment header row)
