---
phase: 03-ai-ioc-extraction
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/intel-extractor/pytest.ini
  - services/intel-extractor/tests/__init__.py
  - services/intel-extractor/tests/conftest.py
  - services/intel-extractor/tests/test_parser.py
  - services/intel-extractor/tests/test_extractor.py
  - services/intel-extractor/tests/test_opencti_client.py
autonomous: true
requirements:
  - AIEX-01
  - AIEX-02
  - AIEX-03
  - AIEX-04
  - AIEX-05

must_haves:
  truths:
    - "pytest services/intel-extractor/tests/ -x -q" exits non-zero (RED phase — all stubs fail or skip)
    - conftest.py defines mock_pycti and mock_ollama fixtures with correct return shapes
    - test_parser.py contains test_extract_pdf_text and test_extract_url_text and test_extract_url_text_trafilatura_fallback
    - test_extractor.py contains test_call_llm_happy_path, test_chunk_text_overlap, test_ioc_dedup_across_chunks
    - test_opencti_client.py contains test_lookup_attack_pattern and test_create_report
  artifacts:
    - services/intel-extractor/pytest.ini
    - services/intel-extractor/tests/__init__.py
    - services/intel-extractor/tests/conftest.py
    - services/intel-extractor/tests/test_parser.py
    - services/intel-extractor/tests/test_extractor.py
    - services/intel-extractor/tests/test_opencti_client.py
  key_links:
    - conftest mock_pycti fixture return shapes must match what opencti_client.py will actually call
    - conftest mock_ollama fixture must use response.message.content attribute path (Assumption A2)
    - test_chunk_text_overlap must assert overlap window content is present in adjacent chunks
---

<objective>
Create the RED-phase test scaffold for intel-extractor. All tests are stubs that import from
modules that do not yet exist — they will fail at import time (or with NotImplementedError) until
Wave 1 and Wave 2 plans implement the production code. This matches the Phase 2 TDD pattern
established in 02-01-PLAN.md.

Purpose: Nyquist compliance — no production code written without a failing test defined first.
Wave 0 also validates Assumption A2 (ollama SDK attribute path) in the conftest mock shape.

Output: 6 files in services/intel-extractor/tests/ + pytest.ini; `pytest -x -q` collects all
tests and fails RED before any production code exists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-ioc-extraction/03-CONTEXT.md
@.planning/phases/03-ai-ioc-extraction/03-RESEARCH.md
@.planning/phases/03-ai-ioc-extraction/03-PATTERNS.md
@.planning/phases/03-ai-ioc-extraction/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest.ini + tests/__init__.py + conftest.py</name>
  <files>
    services/intel-extractor/pytest.ini,
    services/intel-extractor/tests/__init__.py,
    services/intel-extractor/tests/conftest.py
  </files>
  <read_first>
    - services/feed-orchestrator/pytest.ini (copy verbatim)
    - services/feed-orchestrator/tests/conftest.py (mock structure to follow)
    - .planning/phases/03-ai-ioc-extraction/03-PATTERNS.md (conftest fixtures section)
    - .planning/phases/03-ai-ioc-extraction/03-RESEARCH.md (Assumption A2 — ollama attribute path)
  </read_first>
  <action>
    Create services/intel-extractor/pytest.ini as an exact copy of
    services/feed-orchestrator/pytest.ini (4 lines: [pytest], testpaths=tests,
    python_files=test_*.py, python_classes=Test*, python_functions=test_*).

    Create services/intel-extractor/tests/__init__.py as an empty file.

    Create services/intel-extractor/tests/conftest.py with two fixtures:

    Fixture mock_pycti (pytest.fixture): Returns a MagicMock with these pre-configured
    return values:
      - client.indicator.create.return_value = {"id": "indicator--test-uuid-1234"}
      - client.report.create.return_value = {"id": "report--test-uuid-5678"}
      - client.stix_core_relationship.create.return_value = {"id": "relationship--test-uuid-9012"}
      - client.attack_pattern.list.return_value = [{"id": "attack-pattern--test-uuid-3456",
          "name": "Phishing", "x_mitre_id": "T1566"}]

    Fixture mock_ollama (pytest.fixture): Returns a MagicMock where
    client.chat.return_value.message.content equals the JSON string:
    '{"iocs":[{"type":"ip","value":"1.2.3.4"},{"type":"domain","value":"evil.example.com"}],
    "techniques":[{"name":"phishing","description":"email-based lure"}],
    "malware_families":["Emotet"],"threat_actors":[]}'

    The attribute path client.chat.return_value.message.content (not dict access) is
    intentional — this validates Assumption A2 from RESEARCH.md. The conftest is the
    canonical definition of the SDK attribute path for all Phase 3 tests.

    Imports needed: pytest, from unittest.mock import MagicMock
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/intel-extractor && python -c "import pytest; import tests.conftest" 2>&1 || true; python -m pytest tests/conftest.py --collect-only -q 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - services/intel-extractor/pytest.ini exists and contains "testpaths = tests"
    - services/intel-extractor/tests/__init__.py exists (may be empty)
    - services/intel-extractor/tests/conftest.py imports succeed without error
    - mock_pycti fixture: client.attack_pattern.list.return_value[0]["x_mitre_id"] == "T1566"
    - mock_ollama fixture: client.chat.return_value.message.content starts with '{"iocs"'
  </acceptance_criteria>
  <done>pytest.ini copied, tests/__init__.py created, conftest.py defines mock_pycti and mock_ollama with correct return shapes matching pycti 6.4.11 and ollama SDK attribute path.</done>
</task>

<task type="auto">
  <name>Task 2: RED test stubs — test_parser.py, test_extractor.py, test_opencti_client.py</name>
  <files>
    services/intel-extractor/tests/test_parser.py,
    services/intel-extractor/tests/test_extractor.py,
    services/intel-extractor/tests/test_opencti_client.py
  </files>
  <read_first>
    - services/intel-extractor/tests/conftest.py (just created — fixture names)
    - .planning/phases/03-ai-ioc-extraction/03-VALIDATION.md (per-task verification map — exact test function names)
    - .planning/phases/03-ai-ioc-extraction/03-RESEARCH.md (Validation Architecture section — test map table)
    - .planning/phases/03-ai-ioc-extraction/03-CONTEXT.md (decisions D-01 through D-09 — what each test must assert)
  </read_first>
  <action>
    Create three test files. Each test function body raises pytest.importorskip or calls
    pytest.fail("RED: not implemented") to ensure RED phase. Do not use `pass` — a passing
    stub defeats the Nyquist rule.

    services/intel-extractor/tests/test_parser.py:
      Imports: pytest; attempts `from parser import extract_pdf_text, extract_url_text`
      wrapped in try/except ImportError that calls pytest.skip("parser not yet implemented").

      test_extract_pdf_text: Creates a minimal valid PDF bytes fixture inline (b"%PDF-1.4 ..."
      is acceptable as a placeholder — the real test body calls extract_pdf_text(pdf_bytes)
      and asserts the return value is a non-empty string). Body: pytest.fail("RED")

      test_extract_url_text: Calls extract_url_text("https://example.com") and asserts
      return is a non-empty string. Body: pytest.fail("RED")

      test_extract_url_text_trafilatura_fallback: Patches trafilatura.fetch_url to return
      None, then calls extract_url_text("https://example.com") and asserts a non-empty
      string is still returned (fallback path). Body: pytest.fail("RED")

    services/intel-extractor/tests/test_extractor.py:
      Imports: pytest; attempts `from extractor import call_llm, chunk_text, run_extraction`
      wrapped in try/except ImportError that calls pytest.skip("extractor not yet implemented").

      test_call_llm_happy_path(mock_ollama): Calls call_llm(mock_ollama, "llama3.2:3b",
      "The actor used 1.2.3.4") and asserts result["iocs"][0]["type"] == "ip".
      Body: pytest.fail("RED")

      test_chunk_text_overlap: Calls chunk_text("A" * 12000, max_chars=6000,
      overlap_chars=600) and asserts: len(result) >= 2, and result[0][-600:] == result[1][:600]
      (overlap window is identical in adjacent chunks). Body: pytest.fail("RED")

      test_ioc_dedup_across_chunks: Creates a scenario where the same IOC {"type":"ip",
      "value":"1.2.3.4"} appears in two chunks; asserts the dedup logic yields exactly one
      unique IOC. This test should call a deduplicate_iocs helper or test the set-based dedup
      directly. Body: pytest.fail("RED")

    services/intel-extractor/tests/test_opencti_client.py:
      Imports: pytest; attempts `from opencti_client import lookup_attack_pattern,
      create_report, create_indicator` wrapped in try/except ImportError that calls
      pytest.skip("opencti_client not yet implemented").

      test_lookup_attack_pattern(mock_pycti): Calls lookup_attack_pattern(mock_pycti,
      "phishing") and asserts return value == "attack-pattern--test-uuid-3456".
      Body: pytest.fail("RED")

      test_create_report(mock_pycti): Calls create_report(mock_pycti, name="Test Report",
      published="2026-06-25T00:00:00Z", description="test", indicator_ids=["indicator--test-uuid-1234"])
      and asserts return value["id"] == "report--test-uuid-5678".
      Body: pytest.fail("RED")
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/intel-extractor && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - "pytest services/intel-extractor/tests/ -x -q" collects at least 8 test functions
    - All tests either SKIP (ImportError path) or FAIL with "RED" — zero PASS
    - test_parser.py contains exactly these three function names: test_extract_pdf_text, test_extract_url_text, test_extract_url_text_trafilatura_fallback
    - test_extractor.py contains exactly these three function names: test_call_llm_happy_path, test_chunk_text_overlap, test_ioc_dedup_across_chunks
    - test_opencti_client.py contains exactly these two function names: test_lookup_attack_pattern, test_create_report
    - No test file produces a syntax error on import
  </acceptance_criteria>
  <done>8 RED test stubs collected by pytest across 3 files; all stubs fail (ImportError skip or pytest.fail); test names match 03-VALIDATION.md verification map exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production modules | Tests import from modules that do not yet exist — ImportError is expected and handled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-SC | Tampering | pip install in Docker build | high | mitigate | Package Legitimacy Audit in RESEARCH.md verified all packages; all marked OK — no [ASSUMED]/[SUS] packages remain |
</threat_model>

<verification>
Run from project root:
  pytest services/intel-extractor/tests/ -x -q

Expected: all tests SKIP or FAIL (RED phase). Collection count >= 8.
</verification>

<success_criteria>
- 6 files created: pytest.ini, tests/__init__.py, conftest.py, test_parser.py, test_extractor.py, test_opencti_client.py
- pytest collects >= 8 test functions
- Zero passing tests (all RED)
- mock_pycti and mock_ollama fixtures callable from any test file
</success_criteria>

<output>
Create .planning/phases/03-ai-ioc-extraction/03-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

New files:
- `services/intel-extractor/pytest.ini` — test configuration
- `services/intel-extractor/tests/__init__.py` — package marker
- `services/intel-extractor/tests/conftest.py` — fixtures: `mock_pycti`, `mock_ollama`
- `services/intel-extractor/tests/test_parser.py` — stubs: `test_extract_pdf_text`, `test_extract_url_text`, `test_extract_url_text_trafilatura_fallback`
- `services/intel-extractor/tests/test_extractor.py` — stubs: `test_call_llm_happy_path`, `test_chunk_text_overlap`, `test_ioc_dedup_across_chunks`
- `services/intel-extractor/tests/test_opencti_client.py` — stubs: `test_lookup_attack_pattern`, `test_create_report`
</output>
