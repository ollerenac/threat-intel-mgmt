---
phase: 03-ai-ioc-extraction
plan: 03
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - services/intel-extractor/opencti_client.py
autonomous: true
requirements:
  - AIEX-03
  - AIEX-04

must_haves:
  truths:
    - test_lookup_attack_pattern passes GREEN — lookup_attack_pattern(mock_pycti, "phishing") returns "attack-pattern--test-uuid-3456"
    - test_create_report passes GREEN — create_report returns {"id": "report--test-uuid-5678"}
    - create_indicator copied verbatim from Phase 2 with update=True and 3x retry
    - build_pycti_client copied verbatim from Phase 2
    - lookup_attack_pattern returns None (not raises) when attack_pattern.list returns empty list
    - create_relationship uses stix_core_relationship.create with fromId/toId/relationship_type="indicates"/update=True
  artifacts:
    - services/intel-extractor/opencti_client.py
  key_links:
    - _RETRY_DELAYS = [30, 60, 120] constant present — all write functions use it
    - report.create called with report_types=["threat-report"] not report_class= (deprecated)
    - lookup_attack_pattern logs '[extractor] ATT&CK no match for: ...' on empty results (D-09)
---

<objective>
Implement opencti_client.py for intel-extractor. Copy build_pycti_client() and
create_indicator() verbatim from Phase 2, then add three new functions:
lookup_attack_pattern(), create_report(), create_relationship(). All write functions
use the proven 3x retry pattern from Phase 2.

Purpose: Turns test_opencti_client.py stubs from RED to GREEN. Provides the complete
OpenCTI interface needed by extractor.py in Wave 2.

Output: opencti_client.py with 5 functions (2 copied, 3 new), all following Phase 2
retry/logging conventions. Per D-04 and D-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-ioc-extraction/03-CONTEXT.md
@.planning/phases/03-ai-ioc-extraction/03-RESEARCH.md
@.planning/phases/03-ai-ioc-extraction/03-PATTERNS.md
@services/feed-orchestrator/opencti_client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: opencti_client.py — copy Phase 2 base + add 3 new functions</name>
  <files>services/intel-extractor/opencti_client.py</files>
  <read_first>
    - services/feed-orchestrator/opencti_client.py (copy verbatim lines 1–109, then extend)
    - services/intel-extractor/tests/test_opencti_client.py (RED stubs — what must turn GREEN)
    - services/intel-extractor/tests/conftest.py (mock_pycti fixture shapes — what the functions must call)
    - .planning/phases/03-ai-ioc-extraction/03-PATTERNS.md (opencti_client.py section — exact function signatures and retry pattern)
    - .planning/phases/03-ai-ioc-extraction/03-RESEARCH.md (Pattern 3: report.create, Pattern 4: stix_core_relationship.create, Pattern 5: attack_pattern.list, Pitfall 1: create_report before all indicators done)
    - .planning/phases/03-ai-ioc-extraction/03-CONTEXT.md (D-04, D-05, D-08, D-09)
  </read_first>
  <behavior>
    - build_pycti_client(): returns OpenCTIApiClient(url=OPENCTI_URL, token=OPENCTI_TOKEN, log_level="error")
    - create_indicator(): identical to Phase 2 — 3x retry with _RETRY_DELAYS, update=True, returns None on exhaustion
    - lookup_attack_pattern(client, keyword) -> Optional[str]: calls client.attack_pattern.list(search=keyword, first=5); if empty list, logs "[extractor] ATT&CK no match for: '{keyword}' — skipping" at INFO and returns None; else returns results[0]["id"]
    - create_report(client, name, published, description, indicator_ids) -> Optional[dict]: 3x retry; calls client.report.create(name=name, published=published, description=description, objects=indicator_ids, report_types=["threat-report"], update=True)
    - create_relationship(client, from_id, to_id, relationship_type="indicates") -> Optional[dict]: 3x retry; calls client.stix_core_relationship.create(fromId=from_id, toId=to_id, relationship_type=relationship_type, update=True)
  </behavior>
  <action>
    Create services/intel-extractor/opencti_client.py:

    Header docstring: list all 5 public functions.
    Imports: logging, time, datetime/timezone, typing.Optional, pycti.OpenCTIApiClient,
    config.OPENCTI_TOKEN, config.OPENCTI_URL.

    Copy verbatim from services/feed-orchestrator/opencti_client.py:
      - _RETRY_DELAYS = [30, 60, 120] constant
      - build_pycti_client() — no changes
      - create_indicator() — no changes (keep objectLabel, x_opencti_score, all params)

    Add new function lookup_attack_pattern(client, keyword: str) -> Optional[str]:
      No retry (read-only query). Call client.attack_pattern.list(search=keyword, first=5).
      If result is falsy (empty list or None), log at INFO level:
      "[extractor] ATT&CK no match for: '%s' — skipping" with keyword interpolated, return None.
      Else return results[0]["id"] — the internal OpenCTI UUID.
      Do NOT return results[0]["x_mitre_id"] — that is the Txxxx string, for logging only (D-08).

    Add new function create_report(client, name: str, published: str, description: str,
    indicator_ids: list[str]) -> Optional[dict]:
      Use _RETRY_DELAYS loop (same pattern as create_indicator).
      Call client.report.create(name=name, published=published, description=description,
      objects=indicator_ids, report_types=["threat-report"], update=True).
      Warning log prefix: "[opencti_client] report create".
      Return None after all retries exhausted.

    Add new function create_relationship(client, from_id: str, to_id: str,
    relationship_type: str = "indicates") -> Optional[dict]:
      Use _RETRY_DELAYS loop.
      Call client.stix_core_relationship.create(fromId=from_id, toId=to_id,
      relationship_type=relationship_type, update=True).
      Warning log prefix: "[opencti_client] relationship create".
      Return None after all retries exhausted.
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/intel-extractor && python -m pytest tests/test_opencti_client.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_opencti_client.py -x -q` exits 0 with both tests PASSING (GREEN)
    - `python -c "from opencti_client import build_pycti_client, create_indicator, lookup_attack_pattern, create_report, create_relationship; print('imports OK')"` exits 0
    - `grep -c "report_types" services/intel-extractor/opencti_client.py` returns >= 1 (not report_class)
    - `grep -c "report_class" services/intel-extractor/opencti_client.py` returns 0
    - `grep -c "_RETRY_DELAYS" services/intel-extractor/opencti_client.py` returns >= 3 (constant + used in create_report + create_relationship)
    - `grep -c "stix_core_relationship" services/intel-extractor/opencti_client.py` returns >= 1
    - `grep -c "update=True" services/intel-extractor/opencti_client.py` returns >= 3 (indicator, report, relationship)
  </acceptance_criteria>
  <done>5 functions implemented; test_lookup_attack_pattern and test_create_report GREEN; all write functions use 3x retry; report_types used (not deprecated report_class); update=True on all writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor.py → opencti_client.py | Caller provides indicator IDs and technique names — validated upstream |
| opencti_client.py → OpenCTI GraphQL | pycti sends mutations over tim-network; token in env var only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | OPENCTI_TOKEN in config | high | mitigate | Token read from env var only, logged as bool() only — same pattern enforced in Phase 2 |
| T-03-03-02 | Tampering | create_indicator update=True | low | accept | Idempotent upsert is intentional; malicious update requires a valid OpenCTI token which is protected |
| T-03-03-03 | Denial of Service | 3x retry with 30/60/120s delays | low | accept | Demo scope — single-node deployment; retry storm limited to 3 attempts before skipping |
</threat_model>

<verification>
Run from project root:
  pytest services/intel-extractor/tests/test_opencti_client.py -x -q

Expected: 2 tests PASS (GREEN). Then run full suite:
  pytest services/intel-extractor/tests/ -x -q

Expected: test_parser.py (3 PASS), test_opencti_client.py (2 PASS), test_extractor.py still SKIP/FAIL.
</verification>

<success_criteria>
- 5 functions in opencti_client.py: build_pycti_client, create_indicator, lookup_attack_pattern, create_report, create_relationship
- test_lookup_attack_pattern and test_create_report both GREEN
- _RETRY_DELAYS reused across all write functions
- No deprecated pycti parameters (report_class removed, report_types used)
</success_criteria>

<output>
Create .planning/phases/03-ai-ioc-extraction/03-03-SUMMARY.md when done.
</output>

## Artifacts this phase produces

New files:
- `services/intel-extractor/opencti_client.py` — functions: `build_pycti_client() -> OpenCTIApiClient`, `create_indicator(client, name, pattern, observable_type, confidence, labels, source_name, valid_from) -> Optional[dict]`, `lookup_attack_pattern(client, keyword: str) -> Optional[str]`, `create_report(client, name, published, description, indicator_ids) -> Optional[dict]`, `create_relationship(client, from_id, to_id, relationship_type) -> Optional[dict]`
