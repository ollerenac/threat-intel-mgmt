---
phase: 04-semantic-search-engine
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified: []
autonomous: false
requirements:
  - AISEM-01
  - AISEM-02
  - AISEM-03
  - AISEM-04

must_haves:
  truths:
    - semantic-engine container starts and reaches healthy state
    - GET /health returns 200 with status=ok and index_status progressing from starting to indexing to ready
    - After indexing completes, indexed count matches total count
    - GET /search?q=<natural language query> returns results with score between 0.0 and 1.0
    - Each search result includes opencti_url pointing to localhost:8080
    - embedded_text field present in results (analyst can see why IOC matched)
  artifacts: []
  key_links:
    - BC-01: docker compose must use --profile platform --profile semantic (ChromaDB is in platform profile)
    - semantic-engine at localhost:8002; ChromaDB at localhost:8000 (platform profile)
---

<objective>
Integration checkpoint: bring the semantic-engine up against the live platform stack, observe
initial indexing, and run a natural-language search to confirm all 4 AISEM requirements are met.

Purpose: Verifies the full pipeline works end-to-end — OpenCTI → pycti → Ollama → ChromaDB → /search.
Output: Human-confirmed that AISEM-01 through AISEM-04 are satisfied in the live environment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-semantic-search-engine/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build semantic-engine image and verify unit tests pass in container</name>
  <files></files>
  <read_first>
    - services/semantic-engine/requirements.txt (confirm pycti==6.4.11, chromadb==1.5.9 pinned)
    - services/semantic-engine/Dockerfile (confirm port 8002, no libmagic)
  </read_first>
  <action>
    Run docker build for the semantic-engine service and verify the unit test suite passes inside
    the built image before attempting live integration.

    Build command: docker build -t semantic-engine-final ./services/semantic-engine

    Run tests inside image: docker run --rm semantic-engine-final python -m pytest tests/ -v

    Expect: 8 tests PASSED, 0 failed, 0 skipped. If any test fails, stop and fix before proceeding
    to live integration.

    Also confirm the image starts uvicorn without crashing (no import errors at startup):
    docker run --rm -e OPENCTI_URL=http://localhost:8080 -e OPENCTI_TOKEN=test
    -e CHROMADB_URL=http://localhost:8000 -e OLLAMA_URL=http://localhost:11434
    semantic-engine-final uvicorn main:app --host 0.0.0.0 --port 8002
    Wait 3 seconds, then Ctrl+C. Expect: "Application startup complete." in output, no ImportError.
  </action>
  <verify>
    <automated>docker build -t semantic-engine-final /home/researcher/Research/threat_int_mgmt/services/semantic-engine && docker run --rm semantic-engine-final python -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - docker build exits 0
    - docker run python -m pytest tests/ -q exits 0 with "8 passed" in output
    - No "FAILED" or "ERROR" in pytest output
    - uvicorn startup produces "Application startup complete." (no ImportError in startup logs)
  </acceptance_criteria>
  <done>Image builds cleanly; 8 unit tests pass inside container; uvicorn starts without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Live integration — verify AISEM-01 through AISEM-04 against running stack</name>
  <action>Human verification — follow how-to-verify steps below. No automated action required.</action>
  <what-built>
    The semantic-engine service (FastAPI + ChromaDB indexer + Ollama embeddings) is fully built.
    It has been verified with unit tests. Now it needs to run against the live platform stack
    and perform real indexing and search against the OpenCTI indicators loaded by Phases 2 and 3.
  </what-built>
  <how-to-verify>
    Step 1 — Start the full stack with the semantic profile:
    cd /home/researcher/Research/threat_int_mgmt
    docker compose --profile platform --profile semantic up -d
    (Note: --profile platform is required — ChromaDB is in the platform profile per BC-01)

    Step 2 — Watch indexing progress (initial index of all OpenCTI indicators):
    watch -n5 'curl -s http://localhost:8002/health'
    Wait until index_status changes from "starting" → "indexing" → "ready".
    The indexed count should eventually equal the total count.

    Step 3 — Confirm AISEM-01 (all indicators indexed):
    curl -s http://localhost:8002/health | python3 -m json.tool
    Expected: {"status": "ok", "index_status": "ready", "indexed": N, "total": N}
    where indexed == total and N > 0.

    Step 4 — Confirm AISEM-02, AISEM-03, AISEM-04 (natural-language search):
    curl -s "http://localhost:8002/search?q=malware+botnet+C2+server" | python3 -m json.tool
    Expected:
    - "count" > 0 (results returned)
    - Each result has "score" field between 0.0 and 1.0 (AISEM-03)
    - Each result has "opencti_url" containing "localhost:8080/dashboard/observations/indicators/" (AISEM-04)
    - Each result has "embedded_text" field (D-08 — analyst can see why IOC matched)
    - Results appear semantically related to botnet C2 (AISEM-02)

    Step 5 — Verify deep-link works:
    Copy one opencti_url from the results above and open it in a browser.
    Expected: Opens the corresponding indicator in OpenCTI at localhost:8080.

    Step 6 — Confirm docker compose ps shows semantic-engine as healthy:
    docker compose --profile platform --profile semantic ps
    Expected: semantic-engine shows "healthy" status.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 steps pass, or describe any failures so they can be diagnosed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl test caller → /search | Simulates Phase 6 dashboard calling the search endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | /health exposing indexed/total counts | low | accept | Internal-only service; count metadata is non-sensitive operational data |
</threat_model>

<verification>
Human checkpoint gates on 6 verification steps above. All 4 AISEM requirements must be
observable in the live environment before Phase 4 is marked complete.
</verification>

<success_criteria>
- docker compose ps shows semantic-engine as healthy (DEPL-04 partial)
- GET /health returns index_status: "ready" with indexed == total (AISEM-01)
- GET /search?q=<natural language> returns results with 0.0–1.0 scores (AISEM-02, AISEM-03)
- opencti_url in results navigates to the correct indicator in OpenCTI (AISEM-04)
- embedded_text present in each result
</success_criteria>

<output>
Create `.planning/phases/04-semantic-search-engine/04-05-SUMMARY.md` when done.

## Artifacts this phase produces

### New service: services/semantic-engine/

| File | Role |
|------|------|
| `services/semantic-engine/Dockerfile` | python:3.12-slim image, port 8002, no libmagic |
| `services/semantic-engine/requirements.txt` | fastapi==0.115.14, chromadb==1.5.9, ollama==0.6.2, pycti==6.4.11 |
| `services/semantic-engine/config.py` | 8 env var constants: OPENCTI_URL, OPENCTI_TOKEN, OPENCTI_BASE_URL, OLLAMA_URL, OLLAMA_EMBED_MODEL, CHROMADB_URL, SIMILARITY_THRESHOLD (float), POLL_INTERVAL_SECONDS (int) |
| `services/semantic-engine/opencti_client.py` | build_pycti_client(), list_all_indicators(client), list_indicators_since(client, since) |
| `services/semantic-engine/indexer.py` | index_state dict, get_collection(), read_watermark(), write_watermark(), build_embed_text(indicator), run_index_loop() async coroutine |
| `services/semantic-engine/searcher.py` | embed_query(text, ollama_client), search(collection, query, ollama_client, n_results, threshold) |
| `services/semantic-engine/main.py` | FastAPI app, lifespan with asyncio.create_task(indexer.run_index_loop()), GET /health, GET /search |
| `services/semantic-engine/pytest.ini` | testpaths = tests |
| `services/semantic-engine/tests/__init__.py` | empty |
| `services/semantic-engine/tests/conftest.py` | mock_pycti, mock_ollama (embeddings=[[float]*768]), mock_chroma (distances=[[0.2,0.8]]) |
| `services/semantic-engine/tests/test_indexer.py` | test_build_embed_text_* (4 tests, AISEM-01) |
| `services/semantic-engine/tests/test_searcher.py` | test_score_conversion, test_threshold_filters, test_search_returns_ranked, test_opencti_url_format (AISEM-02/03/04) |

### Modified infrastructure

| File | Change |
|------|--------|
| `docker-compose.yml` | semantic-engine block: added SIMILARITY_THRESHOLD=0.3, OPENCTI_BASE_URL=http://localhost:8080, healthcheck (urllib.request, 30s/10s/3/60s) |

### Key symbols

| Symbol | Location | Description |
|--------|----------|-------------|
| `index_state` | indexer.py module level | dict with status/indexed/total; main.py spreads into /health |
| `get_collection()` | indexer.py | returns ChromaDB cosine collection; called by main.py /search |
| `build_embed_text(indicator)` | indexer.py | D-01/D-03 string builder; em dash format with description, bracket format without |
| `run_index_loop()` | indexer.py | async coroutine; fires via asyncio.create_task in lifespan |
| `search(collection, query, ...)` | searcher.py | score = 1 - distance; threshold filter; returns D-08 result shape |
| `GET /health` | main.py | {"status":"ok", "index_status":..., "indexed":N, "total":M} |
| `GET /search?q=` | main.py | {"query":..., "results":[...], "count":N}; 400 if empty or >500 chars |
| `COLLECTION_NAME` | indexer.py | "ioc_embeddings" |
| `WATERMARK_ID` | indexer.py | "_watermark_" |
</output>
