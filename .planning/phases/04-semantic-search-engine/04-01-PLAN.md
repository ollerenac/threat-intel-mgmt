---
phase: 04-semantic-search-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/semantic-engine/pytest.ini
  - services/semantic-engine/tests/__init__.py
  - services/semantic-engine/tests/conftest.py
  - services/semantic-engine/tests/test_indexer.py
  - services/semantic-engine/tests/test_searcher.py
autonomous: true
requirements:
  - AISEM-01
  - AISEM-02
  - AISEM-03
  - AISEM-04

must_haves:
  truths:
    - pytest discovers and runs all tests without ImportError crashing the session
    - test_indexer.py SKIP-passes (not fails) when indexer.py does not yet exist
    - test_searcher.py SKIP-passes when searcher.py does not yet exist
    - conftest.py mock_ollama fixture returns MagicMock with embeddings attribute as a list-of-lists
    - conftest.py mock_chroma fixture query() returns distances and metadatas in expected shape
  artifacts:
    - services/semantic-engine/pytest.ini
    - services/semantic-engine/tests/__init__.py
    - services/semantic-engine/tests/conftest.py
    - services/semantic-engine/tests/test_indexer.py
    - services/semantic-engine/tests/test_searcher.py
  key_links:
    - conftest.py fixtures are imported by both test files — fixture names must match exactly
    - mock_ollama.embed() return shape (embeddings=[[float]*768]) must match the production call site in indexer.py and searcher.py
---

<objective>
Create the RED-phase test scaffold for the semantic-engine service. All test functions
use the import-guard + skipif pattern from intel-extractor so they SKIP (not ERROR) when
production modules do not yet exist.

Purpose: Establishes Nyquist sampling contract before any production code is written.
Output: 5 files; `python -m pytest tests/ -q` runs green (all skipped) from inside services/semantic-engine/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-semantic-search-engine/04-VALIDATION.md
@.planning/phases/04-semantic-search-engine/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest.ini + tests/__init__.py + conftest.py</name>
  <files>
    services/semantic-engine/pytest.ini,
    services/semantic-engine/tests/__init__.py,
    services/semantic-engine/tests/conftest.py
  </files>
  <read_first>
    - services/intel-extractor/pytest.ini (exact pattern to copy — 4 lines)
    - services/intel-extractor/tests/conftest.py (fixture structure to replicate)
    - .planning/phases/04-semantic-search-engine/04-PATTERNS.md §conftest.py section (mock shapes)
  </read_first>
  <action>
    Create services/semantic-engine/pytest.ini identical to intel-extractor's: [pytest] block with
    testpaths = tests, python_files = test_*.py, python_classes = Test*, python_functions = test_*.

    Create services/semantic-engine/tests/__init__.py as an empty file.

    Create services/semantic-engine/tests/conftest.py with three fixtures:

    mock_pycti: MagicMock where client.indicator.list.return_value is a list containing one
    indicator dict with keys id="indicator--test-uuid-1234", name="1.2.3.4",
    x_opencti_main_observable_type="IPv4-Addr", description="C2 server",
    objectLabel=[{"value": "botnet-cc"}], updated_at="2026-06-25T00:00:00.000Z".

    mock_ollama: MagicMock where client.embed.return_value is MagicMock(embeddings=[[0.1]*768]).
    The attribute name is embeddings (plural), it is a list of lists, and the first item is a
    768-element float list. This shape validates the production call `response.embeddings[0]`
    (per RESEARCH.md Pitfall 4 — do not use .embedding singular).

    mock_chroma: MagicMock collection where collection.get.return_value = {"ids": [], "metadatas": []},
    and collection.query.return_value = {"distances": [[0.2, 0.8]], "metadatas": [[
    {"ioc_type": "IPv4-Addr", "value": "1.2.3.4", "opencti_url": "http://localhost:8080/dashboard/observations/indicators/indicator--test-uuid-1234", "embedded_text": "IPv4-Addr: 1.2.3.4 [botnet-cc]"},
    {"ioc_type": "Domain-Name", "value": "evil.com", "opencti_url": "http://localhost:8080/dashboard/observations/indicators/indicator--evil-uuid", "embedded_text": "Domain-Name: evil.com"}
    ]], "documents": [["doc1", "doc2"]]}.

    Distance 0.2 → score 0.8 (above threshold 0.3, included).
    Distance 0.8 → score 0.2 (below threshold 0.3, filtered out).
    This shape is what test_searcher.py will assert against.
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/semantic-engine && python -m pytest tests/conftest.py --collect-only -q 2>&1 | grep -E "error|Error|FAILED" | wc -l | grep -c "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - pytest.ini contains `testpaths = tests` and `python_files = test_*.py`
    - conftest.py imports pass without error when pytest loads fixtures (no ModuleNotFoundError)
    - mock_ollama fixture: `mock_ollama.embed.return_value.embeddings` is a list; `mock_ollama.embed.return_value.embeddings[0]` has length 768
    - mock_chroma fixture: `mock_chroma.query.return_value["distances"][0]` equals [0.2, 0.8]
    - tests/__init__.py exists (empty)
  </acceptance_criteria>
  <done>pytest.ini, tests/__init__.py, and conftest.py created; no import errors on collection.</done>
</task>

<task type="auto">
  <name>Task 2: test_indexer.py + test_searcher.py (RED stubs)</name>
  <files>
    services/semantic-engine/tests/test_indexer.py,
    services/semantic-engine/tests/test_searcher.py
  </files>
  <read_first>
    - services/intel-extractor/tests/test_opencti_client.py (import-guard + skipif pattern, lines 1–10)
    - .planning/phases/04-semantic-search-engine/04-PATTERNS.md §test_indexer.py and §test_searcher.py sections
    - .planning/phases/04-semantic-search-engine/04-VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    Create services/semantic-engine/tests/test_indexer.py using the import-guard + skipif pattern.

    Import guard at top: try to import build_embed_text from indexer; set _IMPORT_OK = True on
    success, False on ImportError. Define _skip = pytest.mark.skipif(not _IMPORT_OK, ...).

    Write these four test functions, all decorated with @_skip (AISEM-01 coverage):

    test_build_embed_text_with_description: construct indicator dict with
    x_opencti_main_observable_type="IPv4-Addr", name="1.2.3.4", description="C2 server",
    objectLabel=[{"value": "botnet-cc"}]. Assert result equals
    "IPv4-Addr: 1.2.3.4 — C2 server botnet-cc" (D-01 format with em dash, space after, labels appended).

    test_build_embed_text_no_description: indicator with description=None,
    objectLabel=[{"value": "malware-distribution"}]. Assert result equals
    "IPv4-Addr: 1.2.3.4 [malware-distribution]" (D-03 bracket format, no em dash).

    test_build_embed_text_no_description_no_labels: indicator with description=None,
    objectLabel=[]. Assert result equals "IPv4-Addr: 1.2.3.4 []".

    test_build_embed_text_empty_labels_ignored: indicator with description="Ransomware C2",
    objectLabel=[]. Assert result equals "IPv4-Addr: 1.2.3.4 — Ransomware C2" (strip trailing space).

    Create services/semantic-engine/tests/test_searcher.py using the same import-guard pattern.

    Import guard: try to import search from searcher; set _IMPORT_OK/skip accordingly.

    Write these four test functions, all @_skip (AISEM-02, AISEM-03, AISEM-04 coverage).
    All accept mock_chroma and mock_ollama as parameters (pytest will inject from conftest.py):

    test_score_conversion: call search(mock_chroma, "Russian malware", mock_ollama, threshold=0.3).
    mock_chroma returns distances [0.2, 0.8]. Assert len(results) == 1 (only score 0.8 passes
    threshold), results[0]["score"] == 0.8, results[0]["ioc_type"] == "IPv4-Addr" (AISEM-03).

    test_threshold_filters: call search with threshold=0.9. Assert len(results) == 0 because
    both scores (0.8 and 0.2) are below 0.9 (D-07 threshold filter).

    test_search_returns_ranked: call search with threshold=0.1 so both results pass.
    Assert len(results) == 2. Assert results[0]["score"] > results[1]["score"]
    (higher similarity score first) (AISEM-02).

    test_opencti_url_format: call search with threshold=0.1. Assert
    "indicator--test-uuid-1234" in results[0]["opencti_url"]. Assert
    "embedded_text" in results[0] (D-08). Assert "score" in results[0] (AISEM-03).

    The search() function signature to import: search(collection, query, ollama_client, threshold).
    Planner note: executor must adapt test calls to match the actual signature chosen in searcher.py
    — the key contract is that cosine distance→similarity conversion and threshold filtering work correctly.
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/semantic-engine && python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` exits 0 with all tests showing "s" (skipped), no "F" (failed) or "E" (error)
    - test_indexer.py contains at least 4 test functions all decorated with the skipif marker
    - test_searcher.py contains at least 4 test functions covering score_conversion, threshold_filters, search_returns_ranked, opencti_url_format
    - No test function body raises AssertionError when indexer/searcher modules are absent
  </acceptance_criteria>
  <done>All 8+ tests skip cleanly; `python -m pytest tests/ -q` exits 0 with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → module under test | Mocked — no real ChromaDB/Ollama/OpenCTI calls in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-SC | Tampering | pip install (chromadb, ollama, pycti) | high | mitigate | Package legitimacy audit completed in RESEARCH.md; all packages approved from official org repos |
</threat_model>

<verification>
From inside services/semantic-engine/:
`python -m pytest tests/ -q` — must exit 0, all tests skipped (0 failed, 0 errored)
`python -m pytest tests/ -v` — each test shows SKIPPED with reason "indexer/searcher not yet implemented"
</verification>

<success_criteria>
- pytest.ini discovered; testpaths = tests
- conftest.py loadable with no import errors
- mock_ollama.embed().embeddings[0] is a 768-float list
- mock_chroma.query()["distances"][0] == [0.2, 0.8]
- All 8 test stubs: SKIPPED, not FAILED
</success_criteria>

<output>
Create `.planning/phases/04-semantic-search-engine/04-01-SUMMARY.md` when done.
</output>
