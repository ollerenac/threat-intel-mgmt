---
phase: 04-semantic-search-engine
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - services/semantic-engine/Dockerfile
  - services/semantic-engine/requirements.txt
  - services/semantic-engine/config.py
  - services/semantic-engine/opencti_client.py
  - docker-compose.yml
autonomous: true
requirements:
  - AISEM-01
  - AISEM-04

must_haves:
  truths:
    - docker build succeeds for services/semantic-engine
    - config.py exposes all 8 env vars as module-level constants including SIMILARITY_THRESHOLD and OPENCTI_BASE_URL
    - opencti_client.py list_all_indicators() calls client.indicator.list(getAll=True, first=500)
    - opencti_client.py list_indicators_since() falls back to full fetch if updated_at filter raises
    - docker-compose.yml semantic-engine block has SIMILARITY_THRESHOLD and OPENCTI_BASE_URL env vars
    - docker-compose.yml semantic-engine block has a healthcheck using python urllib.request
  artifacts:
    - services/semantic-engine/Dockerfile
    - services/semantic-engine/requirements.txt
    - services/semantic-engine/config.py
    - services/semantic-engine/opencti_client.py
  key_links:
    - OPENCTI_BASE_URL in config.py is used by indexer.py to construct opencti_url metadata — must default to http://localhost:8080
    - pycti==6.4.11 pin in requirements.txt is mandatory (7.x breaks OpenCTI 6.4.0)
    - chromadb==1.5.9 pin required for 1.x API (configuration= collection syntax)
---

<objective>
Build the service foundation for semantic-engine: Dockerfile, requirements.txt, config.py,
and opencti_client.py. Also add the two missing env vars (SIMILARITY_THRESHOLD, OPENCTI_BASE_URL)
and a healthcheck to the docker-compose.yml stub.

Purpose: All remaining plans can import from config.py and opencti_client.py without modification.
Output: docker build ./services/semantic-engine succeeds; config and pycti client modules importable.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-semantic-search-engine/04-CONTEXT.md
@.planning/phases/04-semantic-search-engine/04-RESEARCH.md
@.planning/phases/04-semantic-search-engine/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + requirements.txt + config.py + opencti_client.py</name>
  <files>
    services/semantic-engine/Dockerfile,
    services/semantic-engine/requirements.txt,
    services/semantic-engine/config.py,
    services/semantic-engine/opencti_client.py
  </files>
  <read_first>
    - services/intel-extractor/Dockerfile (exact analog — copy with 2 changes: remove apt-get libmagic1 line; port 8001 → 8002)
    - services/intel-extractor/requirements.txt (structure to replicate)
    - services/intel-extractor/config.py (env var pattern, token-never-logged pattern)
    - services/intel-extractor/opencti_client.py (pycti client init, _RETRY_DELAYS pattern)
    - .planning/phases/04-semantic-search-engine/04-PATTERNS.md §config.py and §opencti_client.py sections
  </read_first>
  <action>
    Create services/semantic-engine/Dockerfile copied from intel-extractor/Dockerfile with exactly
    two changes: (1) remove the `apt-get install -y libmagic1` line entirely — semantic-engine has
    no file parsing need; (2) change the CMD port from 8001 to 8002. Base image stays python:3.12-slim.
    WORKDIR /app. COPY requirements.txt then RUN pip install --no-cache-dir -r requirements.txt.
    COPY . . CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8002"].

    Create services/semantic-engine/requirements.txt with these pinned packages (one per line):
    fastapi==0.115.14, uvicorn, chromadb==1.5.9, ollama==0.6.2, pycti==6.4.11, pytest, pytest-mock.
    Do NOT include PyPDF2, trafilatura, requests, python-multipart, beautifulsoup4 — those are
    intel-extractor-specific. pycti must be pinned to exactly 6.4.11 (Pitfall 5 — pip installs 7.x otherwise).

    Create services/semantic-engine/config.py following the intel-extractor config.py pattern exactly:
    module-level constants read via os.environ.get() at import time. Include all 8 vars:
    OPENCTI_URL (default "http://opencti:8080"),
    OPENCTI_TOKEN (default ""),
    OPENCTI_BASE_URL (default "http://localhost:8080") — used to build deep-link URLs per AISEM-04,
    OLLAMA_URL (default "http://ollama:11434"),
    OLLAMA_EMBED_MODEL (default "nomic-embed-text"),
    CHROMADB_URL (default "http://chromadb:8000"),
    SIMILARITY_THRESHOLD as float(os.environ.get("SIMILARITY_THRESHOLD", "0.3")) — D-07,
    POLL_INTERVAL_SECONDS as int(os.environ.get("POLL_INTERVAL_SECONDS", "300")).
    Log token presence only: logger.info("OPENCTI_TOKEN configured: %s", bool(OPENCTI_TOKEN)).
    Never log the token value itself.

    Create services/semantic-engine/opencti_client.py following the intel-extractor opencti_client.py
    pattern for client init and retry delays. Include:
    build_pycti_client() returning OpenCTIApiClient(url=OPENCTI_URL, token=OPENCTI_TOKEN, log_level="error").
    list_all_indicators(client) calling client.indicator.list(getAll=True, first=500) and returning the result.
    list_indicators_since(client, since: str) that constructs a FilterGroup dict with key="updated_at"
    and values=[since], then calls client.indicator.list(getAll=True, first=500, filters=filters,
    orderBy="updated_at", orderMode="asc") inside a try/except — on any exception, log a warning
    "updated_at filter failed, falling back to full fetch" and call list_all_indicators(client) instead
    (per RESEARCH.md Open Question 1 / Assumption A1 fallback).
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt && docker build -t semantic-engine-test ./services/semantic-engine 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - docker build ./services/semantic-engine exits 0 with no errors
    - `docker run --rm semantic-engine-test python -c "from config import SIMILARITY_THRESHOLD, OPENCTI_BASE_URL; assert SIMILARITY_THRESHOLD == 0.3; assert OPENCTI_BASE_URL == 'http://localhost:8080'"` exits 0
    - `docker run --rm semantic-engine-test python -c "from opencti_client import build_pycti_client, list_all_indicators, list_indicators_since"` exits 0 (no ImportError)
    - requirements.txt contains pycti==6.4.11 (exact pin, not pycti>=6 or pycti)
    - requirements.txt contains chromadb==1.5.9 (exact pin)
    - Dockerfile CMD uses port 8002
    - Dockerfile has no apt-get libmagic1 line
  </acceptance_criteria>
  <done>docker build succeeds; config.py and opencti_client.py importable inside container.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing env vars + healthcheck to docker-compose.yml semantic-engine block</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml lines 301–323 (the full semantic-engine service block — read before editing)
    - docker-compose.yml lines 285–300 (intel-extractor healthcheck — exact urllib.request pattern to copy)
  </read_first>
  <action>
    Edit the semantic-engine service block in docker-compose.yml. Do NOT touch any other service.

    Add two env vars to the environment list (after the existing POLL_INTERVAL_SECONDS line):
    - SIMILARITY_THRESHOLD=0.3 (D-07 — allows tuning without rebuild)
    - OPENCTI_BASE_URL=http://localhost:8080 (used by indexer.py to build deep-link URLs per AISEM-04;
      the external-facing URL, not the internal http://opencti:8080)

    Add a healthcheck block (after mem_limit: 1g, before depends_on) using the python urllib.request
    pattern established in intel-extractor (python:3.12-slim has no curl):
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8002/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

    Use start_period: 60s (not 30s) because initial indexing may take longer than intel-extractor
    startup — /health returns ok immediately per D-05 but the service needs time to initialize deps.
  </action>
  <verify>
    <automated>docker compose -f /home/researcher/Research/threat_int_mgmt/docker-compose.yml config --quiet 2>&1 | grep -c "error" | grep -c "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 (valid YAML, no parse errors)
    - `grep "SIMILARITY_THRESHOLD" docker-compose.yml` returns a match in the semantic-engine block
    - `grep "OPENCTI_BASE_URL" docker-compose.yml` returns a match in the semantic-engine block (line 301–340 range)
    - `grep -A2 "semantic-engine" docker-compose.yml` output includes "healthcheck"
    - No other service block is modified (intel-extractor, briefing-generator, soc-dashboard blocks unchanged)
  </acceptance_criteria>
  <done>docker-compose.yml parses cleanly; semantic-engine block has SIMILARITY_THRESHOLD, OPENCTI_BASE_URL, and healthcheck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic-engine → OpenCTI | OPENCTI_TOKEN in env var; never logged per config.py pattern |
| semantic-engine → ChromaDB | Internal Docker network only; no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | config.py OPENCTI_TOKEN logging | medium | mitigate | Log bool(OPENCTI_TOKEN) only — never the value; identical to intel-extractor pattern |
| T-04-02-SC | Tampering | pip install during docker build | high | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit; pinned versions prevent silent upgrades |
</threat_model>

<verification>
`docker build -t semantic-engine-test ./services/semantic-engine` — exits 0
`docker compose config --quiet` — exits 0 (valid YAML)
`docker run --rm semantic-engine-test python -m pytest tests/ -q` — all tests skip, 0 failed
</verification>

<success_criteria>
- docker build exits 0 for services/semantic-engine
- config.py exposes SIMILARITY_THRESHOLD=0.3 and OPENCTI_BASE_URL="http://localhost:8080" as defaults
- opencti_client.py: list_all_indicators() uses getAll=True, first=500; list_indicators_since() has try/except fallback
- docker-compose.yml: semantic-engine block has SIMILARITY_THRESHOLD, OPENCTI_BASE_URL, and healthcheck
- pycti==6.4.11 pinned in requirements.txt
</success_criteria>

<output>
Create `.planning/phases/04-semantic-search-engine/04-02-SUMMARY.md` when done.
</output>
