---
context: phase
phase: 06-soc-dashboard
task: complete
total_tasks: complete
status: post-completion — exploratory/improvement work
last_updated: 2026-06-26T23:18:31.785Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Phase 6 (SOC Dashboard) is fully executed and verified. All 4 plans complete, all DASH-01..06
requirements met, committed and signed off. The session pivoted to post-completion exploration:
understanding how the LLM modules work, troubleshooting the briefing pipeline, and starting
an improvement backlog.

Two bugs were found and fixed after phase close:
1. XSS via unvalidated href scheme in ThreatHunt.jsx — fixed with scheme guard (commit 2b1b444)
2. Briefing generator always producing "no threats" — missing `operator:gt` in OpenCTI date
   filter (commit 8eb35d7). Fix committed but container rebuilt from old image — NOW FIXED
   (rebuilt and redeployed, ioc_count_24h returns 10).

Current live state:
- All services up and healthy
- briefing-generator rebuilt with operator:gt fix deployed
- scripts/tim-check.sh diagnostic script added (commit ae7b455)
- ioc_count_24h: 10 confirmed via /stats
- semantic search working (3 results for "malware" with real IOC data)
- Overview tab should now show real IOC count after browser refresh
</current_state>

<completed_work>
This session (Phase 6 execution + post-completion):

Phase execution:
- Plan 06-01: feed-orchestrator FastAPI conversion, /stats on briefing-generator, CORSMiddleware on all 3 services
- Plan 06-02: Vite+React scaffold, Dockerfile, nginx.conf, 4 docker-compose bugs fixed
- Plan 06-03: Overview.jsx (30s poll), ThreatHunt.jsx (GET search + opencti_url href), Briefings.jsx (3s poll), api.js, App.jsx wired
- Plan 06-04: human checkpoint — all smoke tests PASSED, dashboard confirmed at localhost:3000

Post-completion fixes:
- fix(06-03): opencti_url XSS — `href={/^https?:\/\//.test(url) ? url : undefined}` (commit 2b1b444)
- fix(briefing): operator:gt in _make_updated_at_filter() (commit 8eb35d7)
- chore: scripts/tim-check.sh — 6-layer pipeline diagnostic with filler detection (commit ae7b455)
- docker rebuild of briefing-generator to deploy the operator fix
</completed_work>

<remaining_work>
Phase 6 itself: COMPLETE. No remaining plan work.

Post-phase improvement backlog (not yet planned, discussed with user):

MUST-FIX before client use:
- Briefing storage is in-memory (lost on restart) → SQLite or PostgreSQL
- LLM quality: llama3.2:3b → swap OLLAMA_MODEL to llama3.1:8b or mistral:7b (needs more VRAM)
- Only 5 feeds → add 10-15 more (Abuse.ch suite, CIRCL, etc.)
- No alerting → email/Slack webhook on high-confidence IOC

MUST-BUILD (product gaps):
- IOC enrichment (VirusTotal / Shodan per IOC)
- SIEM export (CEF/STIX push to Splunk, Sentinel, QRadar)
- RBAC + audit trail
- IOC detail view in dashboard (inline context panel instead of opening OpenCTI)

NICE TO HAVE:
- Briefing templates (executive vs. technical analyst)
- Threat actor profiles page
- Historical briefing search
- MISP federation bridge

Remaining debug item:
- top_techniques always empty — ATT&CK patterns not in OpenCTI yet (data gap, not a code bug)
  → connector-mitre needs to run and populate OpenCTI with MITRE ATT&CK data
</remaining_work>

<decisions_made>
- TIM is NOT ready to offer to clients as-is — user confirmed this after honest assessment
- Target buyer identified: mid-market SOC (5-20 analysts) with existing OpenCTI/MISP but no AI layer, can't send data to cloud vendors
- Key differentiators: local LLM (data never leaves), semantic search (no open-source TIP has this), low deployment complexity
- Improvement priority: persistence → alerting → more feeds → enrichment → SOAR
</decisions_made>

<blockers>
- top_techniques empty: connector-mitre needs to populate OpenCTI with ATT&CK framework data
  Workaround: none yet — data gap, not a code bug
- docker-compose build requires all profile flags: `--profile platform --profile briefings` (not just `--profile briefings`) due to opencti dependency
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-soc-dashboard/06-VERIFICATION.md` — verification results, all DASH requirements status
2. `scripts/tim-check.sh` — run this first on resume to confirm live system state
3. `services/briefing-generator/generator.py` — `_make_updated_at_filter()` has the operator:gt fix

## Critical Anti-Patterns (do NOT repeat these)

- **SMOKE TEST CONTENT NOT SHAPE**: The 06-04 smoke test checked `'results' in d` but not `len(results) > 0`. Passed even when ChromaDB was empty. → Always assert non-empty for data-dependent checks.
- **RESTART != REBUILD**: `docker compose restart` does NOT pick up code changes when services use `build:`. Must `docker compose --profile X build <svc> && docker compose --profile X up -d <svc>`. → Always verify code is live with `docker exec <cid> grep "<change>" /app/<file>`.
- **HAND OFF HUMAN TESTS IMMEDIATELY**: Attempted to run `docker compose build` inside agent for smoke tests — wasted time, failed due to profile issues. Any test requiring `docker compose up` should be handed to the user with exact commands immediately. → If a test takes <60s for a human and >5min for an agent, hand it off.

## Infrastructure State
- Stack: up and healthy (`docker compose --profile platform --profile feeds --profile semantic --profile briefings --profile dashboard up -d`)
- ollama: llama3.2:3b (2.0 GB) + nomic-embed-text (274 MB) — both loaded, confirmed 42h ago
- briefing-generator: rebuilt with operator:gt fix, ioc_count_24h: 10
- ChromaDB: has indexed IOC data (semantic search returns real results)
- OpenCTI: has indicators (urlhaus IOCs) but no ATT&CK patterns yet
- feed-orchestrator: last urlhaus run showed 47 IOCs collected

<context>
Session started with Phase 6 execution (all 4 plans). After completion, pivoted to exploratory
mode: explaining LLM architecture, troubleshooting why briefings showed empty data, and
discussing what the system needs before it could be offered to clients. User is evaluating
this as a potential client offering. Two real bugs found and fixed post-execution. User wants
to continue improving the system in next sessions, starting with the improvement backlog above.
Context usage at ~70% when pausing.
</context>

<next_action>
Run `./scripts/tim-check.sh` to confirm system state, then pick the first improvement item:
briefing persistence (SQLite) is lowest effort / highest impact for demo stability.
Or: investigate why connector-mitre hasn't populated ATT&CK data (check docker logs).
</next_action>
