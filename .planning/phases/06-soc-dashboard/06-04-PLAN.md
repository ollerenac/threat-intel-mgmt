---
phase: 06-soc-dashboard
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "curl -sf http://localhost:3000 returns HTML containing 'SOC Dashboard'"
    - "curl -sf http://localhost:8001/feeds/status returns JSON with 5 feeds"
    - "curl -sf http://localhost:8003/stats returns JSON with top_techniques key"
    - "CORS Access-Control-Allow-Origin: http://localhost:3000 present on all three backend services"
    - "curl 'http://localhost:8002/search?q=phishing&n_results=3' returns JSON with results key"
    - "POST /generate → poll GET /briefings/{id} → GET /briefings/{id}/pdf returns application/pdf"
  artifacts:
    - .planning/phases/06-soc-dashboard/06-04-SUMMARY.md
  key_links:
    - "All DASH-01..06 requirements verified by automated curl checks before human checkpoint"
---

<objective>
Bring up the full stack and run the integration smoke tests from RESEARCH.md, then present a human checkpoint to visually verify the three dashboard views and confirm DASH-01 through DASH-06 are complete.

Purpose: This is the only plan that requires human interaction — the analyst opens the browser and confirms the dashboard works end-to-end. All automated checks run first so the human checkpoint only asks for visual/interactive confirmation of what curl already proved.

Output: DASH-01..06 all passing. Phase 6 complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-soc-dashboard/06-RESEARCH.md
@.planning/phases/06-soc-dashboard/06-01-SUMMARY.md
@.planning/phases/06-soc-dashboard/06-02-SUMMARY.md
@.planning/phases/06-soc-dashboard/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and start full stack; run automated integration checks</name>
  <files></files>

  <read_first>
    - .planning/phases/06-soc-dashboard/06-RESEARCH.md — Integration Checklist section (exact curl commands)
    - .planning/phases/06-soc-dashboard/06-01-SUMMARY.md — confirm Plan 01 backend changes committed
    - .planning/phases/06-soc-dashboard/06-02-SUMMARY.md — confirm Dockerfile and compose fixes committed
    - .planning/phases/06-soc-dashboard/06-03-SUMMARY.md — confirm views committed and build passes
  </read_first>

  <action>
    Step 1 — Rebuild all modified services (do not use --no-cache unless a previous build is cached with stale layers):
      docker compose build feed-orchestrator briefing-generator semantic-engine soc-dashboard

    Step 2 — Start the full stack (platform + all service profiles + dashboard):
      docker compose --profile platform --profile feeds --profile semantic --profile briefings --profile dashboard up -d

    Step 3 — Wait for all services to become healthy (poll every 10s, timeout 120s):
      docker compose ps  (check status column — all services should show "healthy" or "running")

    Step 4 — Run automated smoke checks from RESEARCH.md Integration Checklist. Execute each check and record PASS/FAIL:

    Check 1 — Dashboard HTML served (DASH-06):
      curl -sf http://localhost:3000 | grep -q "root" && echo "PASS: dashboard HTML" || echo "FAIL: dashboard not serving"

    Check 2 — /feeds/status returns 5 feeds (DASH-01):
      curl -sf http://localhost:8001/feeds/status | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d['feeds'])==5, f'got {len(d[\"feeds\"])}'; print('PASS: feeds/status')"

    Check 3 — /stats returns expected shape (DASH-02):
      curl -sf http://localhost:8003/stats | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'top_techniques' in d and 'ioc_count_24h' in d; print('PASS: /stats')"

    Check 4 — CORS on all three services (D-14):
      curl -sf -H "Origin: http://localhost:3000" http://localhost:8001/health -I | grep -qi "access-control-allow-origin" && echo "PASS: CORS feed-orchestrator" || echo "FAIL: CORS feed-orchestrator"
      curl -sf -H "Origin: http://localhost:3000" http://localhost:8002/health -I | grep -qi "access-control-allow-origin" && echo "PASS: CORS semantic-engine" || echo "FAIL: CORS semantic-engine"
      curl -sf -H "Origin: http://localhost:3000" http://localhost:8003/health -I | grep -qi "access-control-allow-origin" && echo "PASS: CORS briefing-generator" || echo "FAIL: CORS briefing-generator"

    Check 5 — Semantic search returns results (DASH-03):
      curl -sf "http://localhost:8002/search?q=malware&n_results=3" | python3 -c "import sys,json; d=json.load(sys.stdin); print('PASS: search returns', d.get('count', 0), 'results')"

    Check 6 — PDF generation round-trip (DASH-05):
      ID=$(curl -sf -X POST http://localhost:8003/generate -H "Content-Type: application/json" -d '{"period_hours":24}' | python3 -c "import sys,json; print(json.load(sys.stdin)['briefing_id'])")
      echo "briefing_id: $ID"
      (Note: poll status manually after 60-90s — LLM generation takes 30-45s on 4GB VRAM)

    If any Check 1-5 FAIL: diagnose with docker compose logs <service> before proceeding to checkpoint.
    If Check 6 fails (generation error): confirm ollama is running and llama3.2:3b is loaded; generation can be re-triggered from the dashboard UI.
  </action>

  <verify>
    <automated>
      curl -sf http://localhost:3000 | grep -q "root" && echo "PASS: DASH-06 dashboard served" && curl -sf http://localhost:8001/feeds/status | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d['feeds'])==5; print('PASS: DASH-01 feeds/status')" && curl -sf http://localhost:8003/stats | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'top_techniques' in d; print('PASS: DASH-02 /stats')" && curl -sf "http://localhost:8002/search?q=malware&n_results=3" | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'results' in d; print('PASS: DASH-03 search')"
    </automated>
  </verify>

  <acceptance_criteria>
    - docker compose ps shows soc-dashboard, feed-orchestrator, semantic-engine, briefing-generator all running (healthy or running)
    - curl http://localhost:3000 returns HTML with string "root" (React mount point) — DASH-06
    - curl http://localhost:8001/feeds/status returns JSON with exactly 5 feed objects — DASH-01
    - curl http://localhost:8003/stats returns JSON containing "top_techniques" and "ioc_count_24h" — DASH-02
    - curl http://localhost:8002/search?q=malware&n_results=3 returns JSON with "results" key — DASH-03
    - All three services return Access-Control-Allow-Origin header when Origin: http://localhost:3000 sent — D-14
  </acceptance_criteria>

  <done>All 5 automated checks pass. Stack is up. Proceeding to human visual checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full SOC Dashboard at http://localhost:3000 backed by feed-orchestrator (port 8001), semantic-engine (port 8002), and briefing-generator (port 8003). Three views: Overview, Threat Hunt, Briefings.
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in your browser and verify the following:

    **Overview tab (DASH-01, DASH-02):**
    1. Five feed health cards are visible (urlhaus, malwarebazaar, threatfox, feodo, otx). Each shows a name, IOC count, timestamp, and a colored status dot.
    2. A "IOC count (24h)" stat card is visible below the feed grid.
    3. A "Top ATT&CK Techniques" card is visible with up to 5 technique entries (may be empty if no data yet).
    4. The page auto-refreshes every 30 seconds without manual interaction.

    **Threat Hunt tab (DASH-03, DASH-04):**
    5. A search input with placeholder "Search threats by description, IOC, or behaviour…" is visible.
    6. Type "malware" and press Enter (or click "Search"). Results appear as a ranked list with blue score badges.
    7. Click one result row — a new browser tab opens to http://localhost:8080/... (OpenCTI). If OpenCTI is up, the indicator page loads; if not, the URL is still correct (connection refused is expected if OpenCTI platform is not running).

    **Briefings tab (DASH-05):**
    8. A "Last 24 hours" dropdown and "Generate Briefing" button are visible.
    9. Click "Generate Briefing". Button becomes disabled and shows "Generating…" spinner/label.
    10. Wait 60-90 seconds. When done, the briefings list shows the new entry with a "Download PDF" link.
    11. Click "Download PDF" — a PDF file downloads (opens in browser or triggers download).

    **Visual/UX (UI-SPEC):**
    12. Dark background (#0f1117 approximate). Tab bar has an accent blue underline on the active tab.
    13. No console errors in browser DevTools (F12 → Console tab).
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 checks pass, or describe any issues found (e.g., "Overview shows 0 feeds", "PDF download fails")</resume-signal>

  <verify>
    <human-check>All 13 visual/interactive checks above confirmed passing by the analyst</human-check>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → localhost:3000 → localhost:8001/8002/8003 | Cross-origin fetch from nginx-served SPA to FastAPI services |
| Analyst browser → localhost:8080 (OpenCTI deep-link) | External navigation; opened in new tab with rel="noopener noreferrer" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Tampering | IOC values rendered in browser from API responses | high | mitigate | Verified in Task 1 acceptance criteria: no dangerouslySetInnerHTML in any src/ file (Plan 03 grep gate). React text node rendering confirmed at build time. |
| T-06-04-02 | Information Disclosure | CORS policy bypass if allow_origins set to "*" | high | mitigate | Integration Check 4 confirms Access-Control-Allow-Origin header is specific (http://localhost:3000), not wildcard. Blocking check — if CORS check FAILs, do not proceed to human checkpoint. |
| T-06-04-03 | Spoofing | opencti_url deep-link opens arbitrary URL from API response | low | accept | Demo scope. rel="noopener noreferrer" prevents tab-napping (Plan 03 acceptance criterion). URL is always localhost:8080 in dev environment. |
</threat_model>

<verification>
All automated checks documented in Task 1. Human verification covers interactive behaviors that curl cannot test (tab switching, polling behavior, PDF download, OpenCTI deep-link navigation).

DASH requirements mapping:
- DASH-01: Check 2 (feeds/status) + human check steps 1-4
- DASH-02: Check 3 (/stats) + human check steps 1-4
- DASH-03: Check 5 (search) + human check steps 5-7
- DASH-04: human check step 7 (click result → OpenCTI tab)
- DASH-05: Check 6 (PDF round-trip) + human check steps 8-11
- DASH-06: Check 1 (localhost:3000) + human check step 12-13
</verification>

<success_criteria>
- Automated: all 5 curl checks PASS (DASH-01, DASH-02, DASH-03, DASH-05 partial, DASH-06)
- Human: visual confirmation of three working views, polling behavior, PDF download, OpenCTI deep-link
- DASH-01 through DASH-06 all confirmed
- Phase 6 complete — TIM v1.0 fully operational
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Type |
|--------|------|------|
| Phase 6 completion record | .planning/phases/06-soc-dashboard/06-04-SUMMARY.md | SUMMARY |

<output>
Create `.planning/phases/06-soc-dashboard/06-04-SUMMARY.md` when done.

After human checkpoint passes, update ROADMAP.md Phase 6 status to complete and STATE.md to reflect all 6 phases done.
</output>
