---
context: phase
phase: 01-platform-foundation
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-25T04:58:01.860Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: curl -f/-sf trap — all 5 occurrences fixed in commit 4b79772. Do NOT use curl -f against services returning 4xx when healthy.
- [ ] CONSTRAINT: Internal ports (Ollama 11434, ChromaDB 8000) not host-bound — checks must run via docker compose exec.
- [ ] CONSTRAINT: opencti/worker:6.4.0 is required — without it STIX bundles queue forever, ATT&CK count stays 0.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| curl -f against 4xx-healthy endpoints | 5 occurrences across healthchecks + scripts. OpenCTI /health returns 401 when healthy. | blocking | Use native binary or curl -s + grep. Never curl -f without knowing target's HTTP status. |
| Missing opencti/worker | OpenCTI 6.x requires separate worker for RabbitMQ → Elasticsearch. ATT&CK count = 0 without it. | blocking | Always include opencti/worker:<version> matching platform version. |
| Host-side check on internal port | Causes silent hang. | advisory | Check port mappings before writing readiness checks. |
| Missing integer env var | MITRE_INTERVAL absent → int(None) crash-loop on connector-mitre. | advisory | Check all required env vars against connector docs before deploying. |

<current_state>
Phase 1 is 95% done. Platform running with 9 healthy services. Ollama models pulled. Worker processing MITRE ATT&CK import (22k STIX objects in RabbitMQ queue, count was 0 at pause — will rise to 200+). Waiting for verify-platform.sh to confirm completion.
</current_state>

<completed_work>
- Plan 01-01 DONE (d8d1b0a): .gitignore, .env.example, nvidia-toolkit
- Plan 01-02 DONE (24640bc): profile tags, connector-mitre healthcheck
- Plan 01-03 DONE (498dcc3): setup-env.sh, verify-platform.sh, docs/SETUP.md
- Plan 01-04 Task 1 DONE (4b79772): .env generated, 9 services healthy
- Plan 01-04 Task 2 DONE (4b79772): nomic-embed-text + llama3.2:3b pulled
- Plan 01-04 Task 3 IN PROGRESS: worker processing MITRE import
</completed_work>

<remaining_work>
1. Wait for ./scripts/verify-platform.sh → "[verify-platform] Phase 1 complete."
2. Type "phase-1-complete" in Claude (fresh session)
3. Write 01-04-SUMMARY.md + commit
4. gsd-verifier → VERIFICATION.md
5. Mark Phase 1 complete in ROADMAP + STATE
</remaining_work>

<decisions_made>
- opencti healthcheck: node HTTP check (exit 0 if statusCode < 500)
- ollama healthcheck: CMD ollama list
- chromadb healthcheck: bash /dev/tcp port check
- init-models.sh: readiness via docker compose exec (port not host-bound)
- verify-platform.sh: curl -s + grep (no -f flag)
- Added opencti/worker:6.4.0 service
- Added MITRE_INTERVAL=7 to connector-mitre
</decisions_made>

## Infrastructure State
- 9 Docker services healthy: elasticsearch, redis, rabbitmq, minio, opencti, chromadb, ollama, worker, connector-mitre
- Ollama models: llama3.2:3b (2.0GB) + nomic-embed-text (274MB) — pulled
- Worker: consuming RabbitMQ queue (MITRE import in flight since 04:57Z)
- .env: exists, not git-tracked

<next_action>
Run: ./scripts/verify-platform.sh
When done → type "phase-1-complete" in a FRESH Claude session (/clear first).
</next_action>
