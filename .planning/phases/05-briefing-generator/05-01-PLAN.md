---
phase: 05-briefing-generator
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - services/briefing-generator/tests/__init__.py
  - services/briefing-generator/tests/conftest.py
  - services/briefing-generator/tests/test_generator.py
  - services/briefing-generator/tests/test_pdf_renderer.py
  - services/briefing-generator/fonts/DejaVuSans.ttf
  - services/briefing-generator/requirements.txt
  - services/briefing-generator/Dockerfile
autonomous: true
requirements:
  - AIBR-01
  - AIBR-02
  - AIBR-03
  - AIBR-04

must_haves:
  truths:
    - "python -m pytest tests/ -x -q from services/briefing-generator exits 0 (all stubs skip, none error)"
    - "services/briefing-generator/fonts/DejaVuSans.ttf exists and is a valid TTF binary (starts with bytes 0x00 0x01 0x00 0x00)"
    - "services/briefing-generator/Dockerfile builds successfully with docker build"
    - "test_generator.py and test_pdf_renderer.py contain import-guard skip markers matching the intel-extractor pattern"
  artifacts:
    - services/briefing-generator/tests/__init__.py
    - services/briefing-generator/tests/conftest.py
    - services/briefing-generator/tests/test_generator.py
    - services/briefing-generator/tests/test_pdf_renderer.py
    - services/briefing-generator/fonts/DejaVuSans.ttf
    - services/briefing-generator/requirements.txt
    - services/briefing-generator/Dockerfile
  key_links:
    - "conftest.py mock_pycti fixture shape must match the entity dict shape _build_stats_block() expects (x_opencti_score, x_opencti_main_observable_type, objectLabel, x_mitre_id)"
    - "DejaVuSans.ttf must be at services/briefing-generator/fonts/ so Dockerfile COPY fonts/ /app/fonts/ picks it up"
    - "Dockerfile COPY fonts/ /app/fonts/ must appear before COPY . . to make the layer explicit"
---

<objective>
Wave 0: scaffold the test suite and service infrastructure (Dockerfile, requirements.txt, font asset) so all subsequent implementation plans have a runnable test harness from their first commit.

Purpose: Nyquist compliance requires test stubs to pre-exist production code. The DejaVuSans.ttf font is a binary asset that must be committed to the repo — it cannot be installed at runtime — so it belongs in Wave 0 alongside the Dockerfile that COPYs it.

Output: 7 files. Test stubs skip (not fail) when production modules are absent. Dockerfile builds from the same base pattern as intel-extractor (python:3.12-slim + libmagic1), with an explicit COPY fonts/ layer. Font binary committed at services/briefing-generator/fonts/DejaVuSans.ttf.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-briefing-generator/05-CONTEXT.md
@.planning/phases/05-briefing-generator/05-RESEARCH.md
@.planning/phases/05-briefing-generator/05-PATTERNS.md
@.planning/phases/05-briefing-generator/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Font asset + Dockerfile + requirements.txt</name>
  <files>
    services/briefing-generator/fonts/DejaVuSans.ttf,
    services/briefing-generator/Dockerfile,
    services/briefing-generator/requirements.txt
  </files>

  <read_first>
    - services/intel-extractor/Dockerfile (all 7 lines) — copy exactly; change port 8001 → 8003; insert COPY fonts/ /app/fonts/ after pip install line and before COPY . .
    - services/intel-extractor/requirements.txt (all 11 lines) — copy order convention; drop PyPDF2, trafilatura, requests, python-multipart, beautifulsoup4; add fpdf2==2.8.7 and ollama==0.6.2 (pinned)
    - .planning/phases/05-briefing-generator/05-PATTERNS.md lines 312–331 — Dockerfile preferred approach (commit TTF, no wget)
    - .planning/phases/05-briefing-generator/05-RESEARCH.md lines 95–106 — requirements.txt final form
  </read_first>

  <action>
    Step 1 — Download DejaVuSans.ttf and commit it.

    Create directory services/briefing-generator/fonts/ and download the font:

      mkdir -p services/briefing-generator/fonts
      curl -L "https://github.com/dejavu-fonts/dejavu-fonts/releases/download/version_2_37/dejavu-fonts-ttf-2.37.zip" -o /tmp/dejavu.zip
      unzip -j /tmp/dejavu.zip "*/DejaVuSans.ttf" -d services/briefing-generator/fonts/
      rm /tmp/dejavu.zip

    Verify the file is present and ~757 KB. If the URL fails (assumption A3), fall back to extracting from an installed fpdf2 package:
      python3 -c "import fpdf; import pathlib; print(pathlib.Path(fpdf.__file__).parent)"
    then copy DejaVuSans.ttf from the fpdf2 package's fonts/ subdirectory if it ships one, otherwise wget from the dejavu-fonts GitHub releases page directly.

    Step 2 — Write services/briefing-generator/requirements.txt:

      fastapi==0.115.14
      uvicorn
      pycti==6.4.11
      ollama==0.6.2
      fpdf2==2.8.7
      pytest
      pytest-mock

    Step 3 — Write services/briefing-generator/Dockerfile:

      FROM python:3.12-slim
      RUN apt-get update && apt-get install -y --no-install-recommends libmagic1 \
          && rm -rf /var/lib/apt/lists/*
      WORKDIR /app
      COPY requirements.txt .
      RUN pip install --no-cache-dir -r requirements.txt
      COPY fonts/ /app/fonts/
      COPY . .
      CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8003"]

    BC-04: libmagic1 is mandatory — pycti imports python-magic at module level.
    Port is 8003 — matches docker-compose.yml stub (do not change).
    COPY fonts/ before COPY . . makes the font layer dependency explicit.
  </action>

  <verify>
    <automated>
      test -f services/briefing-generator/fonts/DejaVuSans.ttf || (echo "MISSING font" &amp;&amp; exit 1)
      python3 -c "data=open('services/briefing-generator/fonts/DejaVuSans.ttf','rb').read(); assert len(data) > 100000, 'font too small'; print('font OK', len(data), 'bytes')"
      grep -q "fpdf2==2.8.7" services/briefing-generator/requirements.txt
      grep -q "8003" services/briefing-generator/Dockerfile
      grep -q "COPY fonts/" services/briefing-generator/Dockerfile
    </automated>
  </verify>

  <acceptance_criteria>
    - services/briefing-generator/fonts/DejaVuSans.ttf exists, size > 100 KB
    - requirements.txt contains fpdf2==2.8.7, pycti==6.4.11, ollama==0.6.2, fastapi==0.115.14
    - Dockerfile CMD uses port 8003
    - Dockerfile has COPY fonts/ /app/fonts/ layer before COPY . .
    - libmagic1 present in apt-get install line (BC-04)
  </acceptance_criteria>

  <done>Font committed, requirements.txt and Dockerfile written and matching the intel-extractor pattern with port and font-layer changes applied.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffold</name>
  <files>
    services/briefing-generator/tests/__init__.py,
    services/briefing-generator/tests/conftest.py,
    services/briefing-generator/tests/test_generator.py,
    services/briefing-generator/tests/test_pdf_renderer.py
  </files>

  <read_first>
    - services/intel-extractor/tests/ — inspect __init__.py (empty), conftest.py (fixture patterns), any test file for the import-guard+skipif pattern used in Phase 4 (04-01-PLAN.md decision)
    - .planning/phases/05-briefing-generator/05-PATTERNS.md lines 341–417 — exact conftest.py, test_generator.py, test_pdf_renderer.py stub content
    - .planning/phases/05-briefing-generator/05-RESEARCH.md lines 496–521 — Wave 0 gaps list and test-to-requirement map
    - .planning/phases/05-briefing-generator/05-VALIDATION.md lines 39–47 — per-task test IDs and exact pytest node IDs
    - services/semantic-engine/tests/ (if present) — secondary import-guard reference
  </read_first>

  <action>
    Create four files. All test files use the import-guard+skipif pattern from Phase 4: wrap production imports in a try/except ImportError and mark the module with pytest.importorskip or a module-level skipif so tests SKIP (not FAIL) when production modules are absent.

    FILE 1 — tests/__init__.py: empty file.

    FILE 2 — tests/conftest.py: two pytest fixtures.
      - mock_pycti: MagicMock() with .indicator.list, .threat_actor.list, .malware.list, .campaign.list, .attack_pattern.list return values set to minimal dicts matching the field names _build_stats_block() uses:
          indicator: [{"id": "ind-aaa", "name": "1.2.3.4", "x_opencti_score": 85, "x_opencti_main_observable_type": "IPv4-Addr", "objectLabel": [{"value": "finance"}]}]
          threat_actor: [{"id": "ta-aaa", "name": "APT29"}]
          malware: [{"id": "mal-aaa", "name": "QakBot"}]
          campaign: [{"id": "camp-aaa", "name": "Campaign-X"}]
          attack_pattern: [{"id": "ap-aaa", "name": "Phishing", "x_mitre_id": "T1566"}]
      - mock_ollama: MagicMock() with .chat.return_value.message.content set to a 50-word prose string (attribute path matches response.message.content in _call_ollama).

    FILE 3 — tests/test_generator.py: four test stubs. Use try/except at module level to import _build_stats_block, _make_updated_at_filter, _call_ollama, briefings from generator; if ImportError, set a module-level SKIP_REASON. Each test decorated with @pytest.mark.skipif(SKIP_REASON is not None, reason=SKIP_REASON or "").

      Stub functions (bodies are pass or a single minimal assert):
        - test_build_stats_block(mock_pycti): import guard; call _build_stats_block with minimal data dict; assert "Period:" in result
        - test_call_ollama_truncation(monkeypatch): import guard; patch _ollama_client in generator module with mock returning 400-word string; assert len(result.split()) <= 320
        - test_updated_at_filter(): import guard; result = _make_updated_at_filter(72); assert result["filters"][0]["key"] == "updated_at"
        - test_post_generate_returns_immediately(): import guard; use fastapi.testclient.TestClient on app from main; monkeypatch run_generate to a no-op async; POST /generate with {"period_hours": 24}; assert status_code == 200 and "briefing_id" in response JSON

    FILE 4 — tests/test_pdf_renderer.py: one test stub. Try/except import of render_pdf from pdf_renderer; skipif on ImportError. Also check for font file at services/briefing-generator/fonts/DejaVuSans.ttf (or /app/fonts/DejaVuSans.ttf in Docker); skip if missing.

      test_render_pdf_bytes(): call render_pdf({"period_hours": 24, "created_at": "2026-06-26T02:00:00+00:00", "text": "Test briefing."}); assert isinstance(result, bytes) and result[:4] == b"%PDF"

    NOTE on font path in tests: pdf_renderer.py will hardcode FONT_PATH = "/app/fonts/DejaVuSans.ttf". The stub test must either (a) monkeypatch FONT_PATH to the repo-relative path "services/briefing-generator/fonts/DejaVuSans.ttf", or (b) skip if the font is not found at either path. Add a pytest.fixture or conftest helper that patches pdf_renderer.FONT_PATH to the local font path so test_render_pdf_bytes can run outside Docker.
  </action>

  <verify>
    <automated>
      cd /home/researcher/Research/threat_int_mgmt/services/briefing-generator &amp;&amp; python -m pytest tests/ -x -q 2>&amp;1 | tail -5
    </automated>
  </verify>

  <acceptance_criteria>
    - python -m pytest tests/ -x -q exits 0 (all tests skip or pass — none error/fail)
    - test_generator.py contains test_build_stats_block, test_call_ollama_truncation, test_updated_at_filter, test_post_generate_returns_immediately function definitions
    - test_pdf_renderer.py contains test_render_pdf_bytes function definition
    - conftest.py contains mock_pycti and mock_ollama fixtures
    - No test file raises ImportError at collection time
  </acceptance_criteria>

  <done>Wave 0 complete: pytest runs green (skipping), all 5 test stubs registered, conftest fixtures defined, import guards prevent collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → image | Font file committed to repo and COPYed into Docker image — no network fetch at container start |
| host → container | Dockerfile build context is the service directory — no secrets in build args |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering | Dockerfile COPY . . | low | accept | No secrets in service directory; .env excluded by .gitignore; build context is services/briefing-generator/ only |
| T-05-01-02 | Tampering | fonts/DejaVuSans.ttf supply chain | low | mitigate | Font committed to repo from official dejavu-fonts GitHub release; SHA verified by file-size check (>100 KB). If wget fallback used, pin to version_2_37 URL. |
| T-05-01-SC | Tampering | pip install in Dockerfile | high | mitigate | All packages pre-approved in Package Legitimacy Audit (RESEARCH.md); pinned versions (fastapi==0.115.14, pycti==6.4.11, ollama==0.6.2, fpdf2==2.8.7); --no-cache-dir prevents stale layers |
</threat_model>

<verification>
After both tasks complete:
  cd /home/researcher/Research/threat_int_mgmt/services/briefing-generator
  python -m pytest tests/ -v
  # Expected: all tests collected, all skip with "production module not yet implemented" or similar
  docker build -t briefing-generator-test . 2>&1 | tail -3
  # Expected: Successfully built ...
</verification>

<success_criteria>
- pytest exits 0 with all stubs skipping (no errors, no failures)
- docker build succeeds on the Dockerfile
- fonts/DejaVuSans.ttf present in image at /app/fonts/DejaVuSans.ttf (verify: docker run --rm briefing-generator-test test -f /app/fonts/DejaVuSans.ttf)
- All 4 AIBR requirements have stub test coverage registered in test_generator.py and test_pdf_renderer.py
</success_criteria>

<output>
Create .planning/phases/05-briefing-generator/05-01-SUMMARY.md when done.
</output>
