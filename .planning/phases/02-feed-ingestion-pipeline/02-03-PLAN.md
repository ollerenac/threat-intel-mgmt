---
phase: 02-feed-ingestion-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - FEED-06

must_haves:
  truths:
    - feed-orchestrator service in docker-compose.yml has a healthcheck using python + redis ping
    - feed-orchestrator service passes OTX_API_KEY, MALWAREBAZAAR_AUTH_KEY, THREATFOX_AUTH_KEY as env vars
    - feed-orchestrator depends_on opencti (service_healthy) and redis (service_healthy)
    - .env.example contains MALWAREBAZAAR_AUTH_KEY and THREATFOX_AUTH_KEY as optional blank-default vars
    - No ports block added to feed-orchestrator (internal-only service)
    - profiles: [feeds] is preserved unchanged
  artifacts:
    - docker-compose.yml (modified in place — existing stub updated)
    - .env.example (modified in place — two new vars added)
  key_links:
    - healthcheck test command must use python -c with redis.from_url and r.ping() matching the redis package available in the container
    - OTX_API_KEY var connects to feeds/otx.py disabled-if-absent check (D-07)
    - MALWAREBAZAAR_AUTH_KEY and THREATFOX_AUTH_KEY connect to same disabled pattern for those feeds
---

<objective>
Update docker-compose.yml to complete the feed-orchestrator service stub and add two new optional API key vars to .env.example. These are the only infrastructure file changes in Phase 2 — everything else is new files.

Purpose: The feed-orchestrator container cannot start without its healthcheck and env vars being defined. This plan closes that gap by modifying the existing stub at lines 244-259 of docker-compose.yml.
Output: docker-compose.yml with complete feed-orchestrator service definition; .env.example with 3 feed API key vars documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-feed-ingestion-pipeline/02-CONTEXT.md
@.planning/phases/02-feed-ingestion-pipeline/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docker-compose.yml feed-orchestrator stub with healthcheck and env vars</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (read lines 240-265 to see the exact current stub before editing; also read lines 155-170 to see the worker service healthcheck format to copy)
    - .planning/phases/02-feed-ingestion-pipeline/02-PATTERNS.md (section: docker-compose.yml — UPDATE existing feed-orchestrator stub)
    - .planning/phases/02-feed-ingestion-pipeline/02-CONTEXT.md (Claude's Discretion: healthcheck probe command)
    - .planning/phases/02-feed-ingestion-pipeline/02-RESEARCH.md (section: Docker Service Structure)
  </read_first>
  <action>
    Edit docker-compose.yml using a SCOPED replacement — replace ONLY the existing feed-orchestrator service block. Do NOT rewrite the entire file.

    The current stub (lines ~244-259) is:
      feed-orchestrator:
        build: ./services/feed-orchestrator
        profiles: [feeds]
        environment:
          - OPENCTI_URL=http://opencti:8080
          - OPENCTI_TOKEN=${OPENCTI_ADMIN_TOKEN}
          - REDIS_URL=redis://redis:6379
        mem_limit: 1g
        depends_on:
          opencti:
            condition: service_healthy
          redis:
            condition: service_healthy
        networks:
          - tim-network
        restart: unless-stopped

    Replace with the complete block that adds:
    1. Three additional env vars after REDIS_URL:
       - OTX_API_KEY=${OTX_API_KEY}
       - MALWAREBAZAAR_AUTH_KEY=${MALWAREBAZAAR_AUTH_KEY}
       - THREATFOX_AUTH_KEY=${THREATFOX_AUTH_KEY}
    2. A healthcheck block after depends_on:
       healthcheck:
         test: ["CMD", "python", "-c", "import redis; r=redis.from_url('redis://redis:6379'); r.ping()"]
         interval: 30s
         timeout: 10s
         retries: 3
         start_period: 60s

    Structural invariants to preserve (do not change these):
    - build: ./services/feed-orchestrator
    - profiles: [feeds]
    - mem_limit: 1g
    - depends_on with opencti service_healthy and redis service_healthy
    - networks: - tim-network
    - restart: unless-stopped
    - NO ports block (feed-orchestrator is internal only — confirm intel-extractor at line ~265 has ports but feed-orchestrator must NOT)

    Use Edit (scoped replacement) to replace only the existing block. Verify the YAML indentation matches the surrounding services (2-space indent for service name, 4-space for keys).
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt && docker compose config --quiet 2>&1 | head -5 && echo "compose config OK"</automated>
  </verify>
  <done>
    docker compose config exits 0 (valid YAML).
    grep -A 30 "^  feed-orchestrator:" docker-compose.yml shows: healthcheck block present, OTX_API_KEY env var present, MALWAREBAZAAR_AUTH_KEY env var present, THREATFOX_AUTH_KEY env var present.
    No ports block present under feed-orchestrator.
    profiles: [feeds] unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add MALWAREBAZAAR_AUTH_KEY and THREATFOX_AUTH_KEY to .env.example</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (read the entire file to find the OTX_API_KEY line and surrounding context before editing)
    - .planning/phases/02-feed-ingestion-pipeline/02-PATTERNS.md (section: .env.example — ADD new optional API key vars)
  </read_first>
  <action>
    Edit .env.example using a SCOPED replacement — replace ONLY the section containing OTX_API_KEY.

    Find the existing OTX_API_KEY line (it may appear as a standalone line or within a section comment block). Replace the OTX_API_KEY line (and any existing section comment above it) with:

    # ── Feed API Keys (all optional — feeds are disabled if key is absent) ──
    OTX_API_KEY=
    MALWAREBAZAAR_AUTH_KEY=
    THREATFOX_AUTH_KEY=

    Both new vars must use empty default (= with nothing after) — they are genuinely optional.
    Do NOT use placeholder text like REPLACE_WITH_KEY (feeds gracefully disable when key is absent).
    Add inline comments per the existing .env.example style:
    OTX_API_KEY=              # optional — AlienVault OTX feed (free account at otx.alienvault.com)
    MALWAREBAZAAR_AUTH_KEY=   # optional — MalwareBazaar feed (free account at abuse.ch)
    THREATFOX_AUTH_KEY=       # optional — ThreatFox feed (free account at abuse.ch)

    Use Edit (scoped) to replace only the OTX_API_KEY line and insert the two new lines immediately after it.
  </action>
  <verify>
    <automated>grep -c "MALWAREBAZAAR_AUTH_KEY" /home/researcher/Research/threat_int_mgmt/.env.example && grep -c "THREATFOX_AUTH_KEY" /home/researcher/Research/threat_int_mgmt/.env.example</automated>
  </verify>
  <done>
    .env.example contains MALWAREBAZAAR_AUTH_KEY= (blank default).
    .env.example contains THREATFOX_AUTH_KEY= (blank default).
    OTX_API_KEY= line still present and unchanged in value.
    No secrets or real key values appear anywhere in .env.example.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → docker-compose env block | API keys flow from host .env into container environment at compose up time |
| healthcheck probe → Redis | The healthcheck runs python inside the container; it connects to redis:6379 on the internal Docker network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | .env.example committed to git | low | accept | .env.example has blank defaults — no actual secrets. Real .env is gitignored (confirmed Phase 1). |
| T-02-03-02 | Denial of Service | healthcheck failing causes depends_on cascade | low | accept | start_period: 60s gives the container time to start before healthcheck fires; retries: 3 before marking unhealthy. |
| T-02-03-03 | Spoofing | OTX_API_KEY exposed in docker compose config output | low | accept | docker compose config shows env var names but values come from .env which is gitignored. Demo environment; no multi-tenant risk. |
</threat_model>

<verification>
After both tasks complete:
1. `docker compose config --quiet` exits 0
2. `grep -A 35 "^  feed-orchestrator:" docker-compose.yml | grep -c "OTX_API_KEY"` returns 1
3. `grep -A 35 "^  feed-orchestrator:" docker-compose.yml | grep -c "healthcheck"` returns 1
4. `grep -A 35 "^  feed-orchestrator:" docker-compose.yml | grep -c "ports"` returns 0
5. `grep -c "MALWAREBAZAAR_AUTH_KEY" .env.example` returns 1
6. `grep -c "THREATFOX_AUTH_KEY" .env.example` returns 1
</verification>

<success_criteria>
- docker compose config validates without error
- feed-orchestrator service has healthcheck, 6 env vars, no ports block
- .env.example has MALWAREBAZAAR_AUTH_KEY= and THREATFOX_AUTH_KEY= with blank defaults and inline comments
- profiles: [feeds] unchanged in docker-compose.yml
</success_criteria>

<artifacts>
## Artifacts This Phase Produces (Plan 03)

| Artifact | Type | Consumed By |
|----------|------|-------------|
| docker-compose.yml (updated) | config | docker compose up --profile feeds |
| .env.example (updated) | documentation | developers setting up environment |
</artifacts>

<output>
Create `.planning/phases/02-feed-ingestion-pipeline/02-03-SUMMARY.md` when done
</output>
