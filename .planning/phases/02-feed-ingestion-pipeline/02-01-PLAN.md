---
phase: 02-feed-ingestion-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/feed-orchestrator/pytest.ini
  - services/feed-orchestrator/tests/__init__.py
  - services/feed-orchestrator/tests/conftest.py
  - services/feed-orchestrator/tests/test_urlhaus.py
  - services/feed-orchestrator/tests/test_malwarebazaar.py
  - services/feed-orchestrator/tests/test_threatfox.py
  - services/feed-orchestrator/tests/test_feodo.py
  - services/feed-orchestrator/tests/test_otx.py
  - services/feed-orchestrator/tests/test_deduplicator.py
  - services/feed-orchestrator/tests/test_normalizer.py
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-04
  - FEED-05

must_haves:
  truths:
    - All 7 test files exist with failing placeholder tests (no import errors other than missing production modules)
    - conftest.py defines mock_redis, mock_pycti, and sample fixtures for all 5 feeds
    - pytest.ini points testpaths to tests/ and configures python_files=test_*.py
    - Each test file contains at least one test function that asserts the expected normalization behavior
  artifacts:
    - services/feed-orchestrator/pytest.ini
    - services/feed-orchestrator/tests/conftest.py
    - services/feed-orchestrator/tests/test_urlhaus.py
    - services/feed-orchestrator/tests/test_malwarebazaar.py
    - services/feed-orchestrator/tests/test_threatfox.py
    - services/feed-orchestrator/tests/test_feodo.py
    - services/feed-orchestrator/tests/test_otx.py
    - services/feed-orchestrator/tests/test_deduplicator.py
    - services/feed-orchestrator/tests/test_normalizer.py
  key_links:
    - conftest.py fixtures are used by all 7 test files via pytest fixture injection
    - test_threatfox.py must verify ip:port split behavior (Pitfall 2 from RESEARCH.md)
    - test_normalizer.py::test_confidence must use exact D-09 formula values
---

<objective>
Create the test scaffold for the feed-orchestrator service. All 7 test files are written as failing tests (RED phase) before any production code exists. This ensures the Nyquist validation requirement is met and gives executors in Waves 2-3 a clear specification of expected behavior.

Purpose: Establish test contracts before implementation. Failing imports are expected and acceptable — the tests define what the production modules must deliver.
Output: pytest.ini, tests/conftest.py, and 7 test files (one per requirement area).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-feed-ingestion-pipeline/02-RESEARCH.md
@.planning/phases/02-feed-ingestion-pipeline/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pytest.ini and tests/conftest.py with all shared fixtures</name>
  <files>
    services/feed-orchestrator/pytest.ini,
    services/feed-orchestrator/tests/__init__.py,
    services/feed-orchestrator/tests/conftest.py
  </files>
  <read_first>
    - .planning/phases/02-feed-ingestion-pipeline/02-RESEARCH.md (sections: Validation Architecture, Wave 0 Gaps, Code Examples conftest)
    - .planning/phases/02-feed-ingestion-pipeline/02-PATTERNS.md (sections: test conftest.py, pytest.ini)
  </read_first>
  <behavior>
    - pytest.ini sets testpaths=tests, python_files=test_*.py, python_classes=Test*, python_functions=test_*
    - mock_redis fixture: MagicMock with r.set returning True (new key), r.hset returning None, r.hgetall returning {}
    - mock_pycti fixture: MagicMock with client.indicator.create returning {"id": "indicator--test-uuid"}
    - sample_urlhaus_rows fixture: list with one dict containing url="http://evil.example.com/malware.exe", url_status="online", dateadded="2026-06-25 00:00:00", tags="Mozi,exe"
    - sample_malwarebazaar_rows fixture: list with one dict containing sha256_hash="e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855", first_seen="2026-06-25 00:00:00", signature="Emotet", tags=["banking"]
    - sample_threatfox_rows fixture: list with entries for domain, ip:port ("185.220.101.47:4444"), url, md5_hash, sha256_hash ioc_types
    - sample_feodo_rows fixture: list with one dict containing first_seen_utc="2026-06-25 00:00:00 UTC", dst_ip="185.220.101.47", dst_port="4444", malware="Emotet"
    - sample_otx_indicators fixture: list with dicts for types: IPv4, domain, URL, FileHash-MD5, FileHash-SHA256
  </behavior>
  <action>
    Create the directory structure: mkdir -p services/feed-orchestrator/tests

    Write services/feed-orchestrator/pytest.ini with [pytest] section: testpaths = tests, python_files = test_*.py, python_classes = Test*, python_functions = test_*

    Write services/feed-orchestrator/tests/__init__.py as an empty file.

    Write services/feed-orchestrator/tests/conftest.py with these fixtures using pytest and unittest.mock.MagicMock:
    - mock_redis: MagicMock with .set(key, val, nx=True, ex=86400) returning True (simulates new key), .hset() returning None, .hgetall() returning {}
    - mock_pycti: MagicMock with .indicator.create() returning {"id": "indicator--test-uuid"}
    - sample_urlhaus_rows: one-element list per RESEARCH.md URLhaus field table
    - sample_malwarebazaar_rows: one-element list per RESEARCH.md MalwareBazaar field table with sha256_hash, first_seen, signature, tags
    - sample_threatfox_rows: five-element list covering domain, ip:port, url, md5_hash, sha256_hash ioc_types (per RESEARCH.md ThreatFox field table)
    - sample_feodo_rows: one-element list with first_seen_utc, dst_ip, dst_port, malware fields (per RESEARCH.md Feodo field table)
    - sample_otx_indicators: list covering IPv4, domain, URL, FileHash-MD5, FileHash-SHA256 types (per RESEARCH.md OTX indicator type table)
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/feed-orchestrator && python -m pytest tests/conftest.py --collect-only -q 2>&1 | grep -E "(conftest|error|ERROR)" | head -20</automated>
  </verify>
  <done>pytest.ini exists; tests/__init__.py exists; conftest.py exists with 7 fixtures (mock_redis, mock_pycti, sample_urlhaus_rows, sample_malwarebazaar_rows, sample_threatfox_rows, sample_feodo_rows, sample_otx_indicators); no syntax errors on import</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write all 7 RED-phase test files covering FEED-01 through FEED-05</name>
  <files>
    services/feed-orchestrator/tests/test_urlhaus.py,
    services/feed-orchestrator/tests/test_malwarebazaar.py,
    services/feed-orchestrator/tests/test_threatfox.py,
    services/feed-orchestrator/tests/test_feodo.py,
    services/feed-orchestrator/tests/test_otx.py,
    services/feed-orchestrator/tests/test_deduplicator.py,
    services/feed-orchestrator/tests/test_normalizer.py
  </files>
  <read_first>
    - services/feed-orchestrator/tests/conftest.py (just created — read actual fixture names before writing tests)
    - .planning/phases/02-feed-ingestion-pipeline/02-RESEARCH.md (sections: Feed-Specific Reference, STIX 2.1 Pattern Reference, Code Examples, Phase Requirements Test Map)
    - .planning/phases/02-feed-ingestion-pipeline/02-PATTERNS.md (section: test_*.py patterns)
  </read_first>
  <behavior>
    test_urlhaus.py (FEED-01):
    - test_normalize_url_pattern: normalize(sample_urlhaus_rows) returns one dict with pattern == "[url:value = 'http://evil.example.com/malware.exe']"
    - test_normalize_includes_tags: result[0]["labels"] contains "Mozi"
    - test_normalize_skips_empty_url: normalize([{"url": "", "tags": "", "dateadded": ""}]) returns []

    test_malwarebazaar.py (FEED-02a):
    - test_normalize_sha256_pattern: result[0]["pattern"] == "[file:hashes.'SHA-256' = 'e3b0c44...']" (single quotes around SHA-256 — Pitfall 3)
    - test_normalize_includes_malware_family: "Emotet" in result[0]["labels"]

    test_threatfox.py (FEED-02b):
    - test_normalize_ip_port_split: ioc_type="ip:port", ioc="185.220.101.47:4444" produces pattern "[ipv4-addr:value = '185.220.101.47']" (Pitfall 2)
    - test_normalize_domain: ioc_type="domain" produces "[domain-name:value = '...']"
    - test_normalize_sha256_hash: ioc_type="sha256_hash" produces "[file:hashes.'SHA-256' = '...']"
    - test_normalize_skips_unknown_type: ioc_type="email" returns no indicator for that row

    test_feodo.py (FEED-02c):
    - test_normalize_dst_ip_pattern: result[0]["pattern"] == "[ipv4-addr:value = '185.220.101.47']"
    - test_normalize_c2_label_always_present: "c2" in result[0]["labels"]
    - test_normalize_includes_malware_family: "Emotet" in result[0]["labels"]

    test_otx.py (FEED-03):
    - test_ipv4_type_maps_to_ipv4_pattern: indicator with type="IPv4", indicator="1.2.3.4" produces "[ipv4-addr:value = '1.2.3.4']"
    - test_sha256_type_maps_to_sha256_pattern: type="FileHash-SHA256" produces "[file:hashes.'SHA-256' = '...']"
    - test_sha1_type_maps_to_sha1_pattern: type="FileHash-SHA1" produces "[file:hashes.'SHA-1' = '...']"
    - test_unknown_type_is_skipped: type="email" produces no indicator

    test_deduplicator.py (FEED-04):
    - test_first_call_not_duplicate: is_duplicate(mock_redis, "[url:value = 'http://x.com']") returns False when mock_redis.set returns True
    - test_second_call_is_duplicate: is_duplicate(mock_redis, same_pattern) returns True when mock_redis.set returns None (key existed)
    - test_key_uses_sha256_of_pattern: verifies mock_redis.set is called with key starting with "tim:ioc_seen:"

    test_normalizer.py (FEED-05):
    - test_confidence_feodo_new: compute_confidence("feodo", datetime_today, seen_in_feeds=1) == min(100, 1*25 + 10 + 30) == 65
    - test_confidence_otx_7_days_old: compute_confidence("otx", datetime_7_days_ago, seen_in_feeds=1) == min(100, 25 + 3 + 25) == 53
    - test_confidence_caps_at_100: compute_confidence("feodo", datetime_today, seen_in_feeds=3) == 100
    - test_recency_bonus_floors_at_zero: 11+ day old IOC produces recency_bonus=0
  </behavior>
  <action>
    Write each test file. All imports will fail (production modules do not exist yet) — this is the RED phase.
    Use relative imports from the parent package: "from feeds.urlhaus import URLhausFeed", "from deduplicator import is_duplicate", "from normalizer import compute_confidence".
    Each test file must import only from its target module and from conftest fixtures.

    For test_normalizer.py, compute the expected values using the D-09 formula directly in comments so the executor in Wave 3 can verify the formula matches:
    quality_weights: feodo=30, otx=25, threatfox=20, urlhaus=15, malwarebazaar=15
    recency_bonus = max(0, 10 - days_old)
    score = min(100, seen_in_feeds * 25 + recency_bonus + quality_weight)

    For test_deduplicator.py, configure mock_redis.set to return True on first call (new key), None on second call (key existed). Use side_effect=[True, None] or set return_value per test.

    Do NOT write any production code in this task. Tests must be syntactically valid Python but will raise ImportError when collected — that is expected and correct.
  </action>
  <verify>
    <automated>cd /home/researcher/Research/threat_int_mgmt/services/feed-orchestrator && python -m pytest tests/ --collect-only -q 2>&1 | grep -c "test session starts" && echo "syntax OK"</automated>
  </verify>
  <done>
    All 7 test files exist and are syntactically valid Python (no SyntaxError).
    Running pytest --collect-only shows ImportError for missing production modules (feeds.urlhaus, deduplicator, normalizer) — this is expected RED state.
    test_normalizer.py::test_confidence_feodo_new and test_confidence_caps_at_100 exist.
    test_threatfox.py::test_normalize_ip_port_split exists.
    test_deduplicator.py::test_key_uses_sha256_of_pattern exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production modules | Tests import from production modules that don't exist yet — ImportError is expected |
| conftest fixtures → test bodies | Mock objects must not leak real credentials or real Redis connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-SC | Tampering | pip install (test dependencies: pytest, pytest-mock) | high | mitigate | Dependencies declared in requirements.txt (Wave 1, Plan 02); no install in this plan — tests run against host Python until Docker build in Plan 02 |
| T-02-01-01 | Information Disclosure | conftest.py fixtures | low | accept | Fixtures use only hardcoded dummy values (no real API keys, no real Redis) — no secrets at risk |
</threat_model>

<verification>
After both tasks complete:
1. `ls services/feed-orchestrator/tests/` shows: __init__.py, conftest.py, test_urlhaus.py, test_malwarebazaar.py, test_threatfox.py, test_feodo.py, test_otx.py, test_deduplicator.py, test_normalizer.py
2. `ls services/feed-orchestrator/pytest.ini` exists
3. `cd services/feed-orchestrator && python -c "import ast; ast.parse(open('tests/conftest.py').read()); print('syntax OK')"` exits 0
4. `cd services/feed-orchestrator && python -c "import ast; [ast.parse(open(f'tests/{f}').read()) for f in ['test_urlhaus.py','test_threatfox.py','test_deduplicator.py','test_normalizer.py']]; print('syntax OK')"` exits 0
</verification>

<success_criteria>
- pytest.ini present with testpaths=tests
- tests/conftest.py present with 7 fixtures
- All 7 test files present and syntactically valid
- test_threatfox.py contains test for ip:port split behavior
- test_normalizer.py contains test_confidence_feodo_new with expected value 65
- test_deduplicator.py contains assertion that Redis key starts with "tim:ioc_seen:"
</success_criteria>

<artifacts>
## Artifacts This Phase Produces (Plan 01)

| Artifact | Type | Consumed By |
|----------|------|-------------|
| services/feed-orchestrator/pytest.ini | config | pytest runner in all later plans |
| services/feed-orchestrator/tests/conftest.py | test fixture | all 7 test files |
| services/feed-orchestrator/tests/test_urlhaus.py | test spec (RED) | Plan 04 URLhausFeed implementation |
| services/feed-orchestrator/tests/test_malwarebazaar.py | test spec (RED) | Plan 05 MalwareBazaarFeed implementation |
| services/feed-orchestrator/tests/test_threatfox.py | test spec (RED) | Plan 05 ThreatFoxFeed implementation |
| services/feed-orchestrator/tests/test_feodo.py | test spec (RED) | Plan 04 FeodoFeed implementation |
| services/feed-orchestrator/tests/test_otx.py | test spec (RED) | Plan 06 OTXFeed implementation |
| services/feed-orchestrator/tests/test_deduplicator.py | test spec (RED) | Plan 02 deduplicator.py |
| services/feed-orchestrator/tests/test_normalizer.py | test spec (RED) | Plan 07 normalizer.py |
</artifacts>

<output>
Create `.planning/phases/02-feed-ingestion-pipeline/02-01-SUMMARY.md` when done
</output>
