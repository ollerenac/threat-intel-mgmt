---
context: research
status: in_progress
last_updated: 2026-07-01T22:06:07.686Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_None._

<current_state>
Session 2026-07-01 afternoon. All 9 TIM phases complete (milestone v1.1 shipped). This session diagnosed 3 empty OpenCTI dashboard widgets. No code was written — diagnosis only. Next session implements the fix.
</current_state>

<completed_work>

- Phase 9 (doc-collector): fully executed, verified, marked complete (see prior handoff)
- OpenCTI widget diagnosis: identified root cause for all 3 empty widgets (see below)
</completed_work>

<remaining_work>

**Fix the 3 empty OpenCTI dashboard widgets:**

1. **Most Targeted Victims** — 0 `targets` relationships; only 1 Identity object (MITRE Corp). Need Sector/Org objects + `intrusion-set → targets → Sector` relationships.

2. **Most Active Vulnerabilities** — 77,002 CVEs present but 0 `exploits` relationships. Need `threat-actor/malware → exploits → CVE-XXXX` relationships. Easiest fix: CISA KEV connector.

3. **Targeted Countries** — 57 Country objects present but 0 `targets` relationships. Need `intrusion-set → targets → Country` relationships.
</remaining_work>

<decisions_made>
- Diagnosis complete: root cause is missing STIX relationship objects, not missing entities
- 77,002 CVEs in OpenCTI — entity data is fine, relationships are absent
- 57 countries in OpenCTI — entity data is fine, relationships are absent
- connector-mitre and connector-cve do not create relationship objects
</decisions_made>

<blockers>
User needs to decide which fix path to pursue before implementation begins (see options below).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full diagnosis details in `context_notes`
2. `docker-compose.yml` — current connector setup (connector-mitre, connector-cve, connector-ipinfo)

## Fix Options

### Option A — CISA KEV connector (recommended, fastest)
Fixes: **Most Active Vulnerabilities**
- Add `opencti/connector-cisa-known-exploited-vulnerabilities` to docker-compose.yml
- Creates `exploits` relationships for ~1,000 actively exploited CVEs
- One service addition, no data import needed

### Option B — STIX bundle with targeting relationships
Fixes: **Most Targeted Victims** + **Targeted Countries**
- Build a STIX 2.1 bundle with Sector objects (Government, Finance, Healthcare, Energy) and Country objects linked via `targets` relationships to the existing ATT&CK intrusion sets
- Can be imported via OpenCTI UI (Data → Import) or TAXII push
- Could be generated from MITRE ATT&CK group pages which list targeted sectors/regions

### Option C — Both A + B
Fixes all 3 widgets.

## Infrastructure State
- OpenCTI: http://localhost:8080 (healthy, up 7h)
- worker: healthy
- connector-mitre: healthy (ATT&CK data loaded)
- connector-cve: up (77,002 CVEs ingested)
- connector-ipinfo: up
- All TIM services running (intel-extractor:8004, soc-dashboard:443, etc.)

## Verified OpenCTI Data Counts
- Intrusion Sets: 185
- Malware: 851
- Indicators: 34,700
- Vulnerabilities: 77,002
- Countries: 57
- Sectors/Orgs: 1 (only MITRE Corp)
- `targets` relationships: **0**
- `exploits` relationships: **0**

<context>
No GSD phase needed for this work — it's connector configuration + optional STIX data import. Not a multi-plan phase, just a docker-compose.yml addition and/or a data import step.
</context>

<next_action>
Decide on fix option (A, B, or C), then:
- Option A: add CISA KEV connector to docker-compose.yml and `docker compose up -d connector-cisa-kev`
- Option B: generate/import STIX bundle with sector + country targeting relationships
- Option C: both
</next_action>
