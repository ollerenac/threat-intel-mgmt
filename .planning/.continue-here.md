---
context: phase
phase: 02-feed-ingestion-pipeline
task: 0
total_tasks: 0
status: ready-to-plan
last_updated: 2026-06-25T05:55:20.107Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session. Phase 1 advisory anti-patterns carry forward below._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| curl -f against 4xx-healthy endpoints | OpenCTI /health returns HTTP 401 when healthy; `-f`/`-sf` treats 4xx as failure causing infinite polling loops | advisory | Use `curl -s \| grep -qE 'unauthorized\|ok'` pattern; test probe manually before writing healthcheck |
| Host-side probe of internal-only ports | Ollama port 11434 is not host-bound by design; host curl always fails | advisory | Probe internal services with `docker compose exec <svc> <cmd>` not host-side curl |
| Assuming image has curl/wget | opencti, ollama, chromadb images ship without curl/wget; probes fail silently at healthcheck eval | advisory | Run `docker compose exec <svc> which curl` before writing any healthcheck that uses curl |
| Omitting opencti/worker | OpenCTI 6.x requires a separate worker process; without it STIX bundles sit in RabbitMQ forever | advisory | Any OpenCTI 6.x compose stack must include `opencti/worker` — already present, do not remove it |

<current_state>
Phase 2 (Feed Ingestion Pipeline) context fully gathered and committed. Session ended at a clean boundary.

What was done this session:
- Resumed from prior between-phases pause (consumed HANDOFF.json, .continue-here.md)
- Ran /gsd-discuss-phase 2: gathered 8 implementation decisions across 4 gray areas
- Wrote .planning/phases/02-feed-ingestion-pipeline/02-CONTEXT.md (commit 0c6b7a4)
- Wrote .planning/phases/02-feed-ingestion-pipeline/02-DISCUSSION-LOG.md (same commit)
- Updated STATE.md (commit 0a52fca)

Platform is still running: 9/9 Docker services healthy at localhost:8080.
Nothing needs to be started or recovered — pure forward motion next session.
</current_state>

<completed_work>
This session (2026-06-25):
- /gsd-resume-work: restored full context from prior session HANDOFF.json
- /gsd-discuss-phase 2: 4 gray areas discussed and decided (feed health storage, failure behavior, startup, STIX scope)
- 02-CONTEXT.md written with 9 implementation decisions (D-01 through D-09 + Claude's discretion block)
- 02-DISCUSSION-LOG.md written as audit trail
- STATE.md updated: "Phase 2 context gathered — ready to plan"
- HANDOFF.json + .continue-here.md from prior session consumed and deleted

All commits are clean. Working tree is empty.
</completed_work>

<remaining_work>
Immediate next steps (both unblocked, no dependencies between them):

1. `/gsd-plan-phase 2` — Feed Ingestion Pipeline
   - feed-orchestrator service: URLhaus, MalwareBazaar, ThreatFox, Feodo Tracker, AlienVault OTX
   - Context is in 02-CONTEXT.md — researcher and planner must read it
   - Requirements: FEED-01 through FEED-06
   - ⚠️ OTX feed task needs API key before execution (not before planning)

2. `/gsd-discuss-phase 3` then `/gsd-plan-phase 3` — AI IOC Extraction (parallel with Phase 2)
   - intel-extractor service: PDF + URL input → local LLM → IOC extraction → STIX → OpenCTI
   - MITRE ATT&CK technique mapping from extracted IOCs
   - Requirements: AIEX-01 through AIEX-05
   - No CONTEXT.md yet — discuss-phase recommended first

Phase 2 and Phase 3 can be planned in the same session (no dependency between them).
</remaining_work>

<decisions_made>
Phase 2 decisions locked in 02-CONTEXT.md:

Feed health state:
- Redis hash keys: tim:feed_status:{feed_name}
- Fields: last_run, ioc_count, status (ok/error/running/never_run/disabled), error_msg
- Pure background worker — no HTTP endpoint, no FastAPI needed

Feed failure handling:
- Download failure: retry 3× exponential backoff (30s → 60s → 120s), then skip + mark status=error
- pycti insertion failure: same 3× backoff, then skip (IOCs lost for that run; next run re-downloads)
- Other feeds always continue regardless of individual failure

Startup behavior:
- Run all feeds immediately at container start, then schedule on cadences
- OTX_API_KEY absent: log warning, mark status=disabled, skip OTX, run other 4 feeds normally

STIX modeling:
- Indicator SDOs only (no malware SDOs in Phase 2)
- Malware family names → indicator labels field
- Confidence weights: Feodo=30, OTX=25, ThreatFox=20, URLhaus=15, MalwareBazaar=15
- Formula: min(100, feed_count × 25 + recency_bonus + quality_weight)
</decisions_made>

<blockers>
None. Platform is stable and running. All decisions locked. Clean boundary.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-feed-ingestion-pipeline/02-CONTEXT.md` — 8 locked decisions for Phase 2; planner and researcher MUST read before planning
2. `docs/plans/2026-06-23-tim-system-design.md` §4.2 — Feed Orchestrator component spec (pipeline, feed table, cadences, tech stack)
3. `.planning/REQUIREMENTS.md` — FEED-01 through FEED-06 (Phase 2 acceptance criteria)
4. `docker-compose.yml` — existing service patterns (healthcheck, depends_on, network, profiles) to follow
5. `.planning/ROADMAP.md` — Phase 2 success criteria and Phase 3 goal (for parallel planning context)

## Infrastructure State
- Docker Compose stack: 9/9 services healthy at `localhost:8080`
- OpenCTI: admin credentials in `.env` (`OPENCTI_ADMIN_EMAIL` / `OPENCTI_ADMIN_PASSWORD` / `OPENCTI_ADMIN_TOKEN`)
- 709 MITRE ATT&CK attack patterns loaded in OpenCTI
- TAXII 2.1 collection: "MITRE ATT&CK" UUID `c8baaf8d-08d0-44dd-87cb-ce7d4d9eac50`
- Ollama: `llama3.2:3b` + `nomic-embed-text` ready
- Redis: running at `redis://redis:6379` (shared, available for feed-orchestrator)
- Git: master branch, latest commit 0ea66e4, working tree clean

<context>
Session ended at the cleanest possible point — Phase 2 context gathered, no in-flight work, no partial artifacts.

Next session is purely additive: plan Phase 2 (and optionally discuss + plan Phase 3 in the same session). The CONTEXT.md is thorough enough that the planner can move quickly. Phase 2 is the more complex plan (5 feed parsers, normalization pipeline, Redis dedup, APScheduler scheduling); Phase 3 is more self-contained (single FastAPI service + local LLM).

CIRCL MISP was explicitly deferred during discussion — do not include it in Phase 2 scope.
Malware SDOs were explicitly deferred — do not include them in Phase 2 scope.
</context>

<next_action>
Start with: `/gsd-plan-phase 2` (Phase 2 context is ready in 02-CONTEXT.md)
Optional first: `/gsd-discuss-phase 3` to gather Phase 3 context before planning it
</next_action>
